# 進捗ログ（詳細アーカイブ）

このファイルは `CLAUDE.md` の「進捗ログ（申し送り）」が長くなりすぎたため、2026-09-07に分離した詳細版。
**CLAUDE.mdには現在の状態の要約だけを置き、「いつ・なぜ・どう直したか」という経緯の詳細はこちらに時系列で記録する。**
過去の不具合の原因調査や、同じ種類のバグが再発したときの参考にする用途を想定している。日々の作業でどこから読むか迷ったら、まず`CLAUDE.md`の「現状まとめ」「残タスク一覧」を見て、経緯が知りたくなったらこのファイルを検索する、という使い方を想定。

以下、時系列（項目番号はほぼ時系列だが、途中で「### 次回以降にやること」という別立てのセクションから始まった経緯があり、一部前後する）。

### これまでにやったこと

- **GitHub連携**：`git init` 済み。リモートは `https://github.com/weedy-seadragon/uec-credit-route.git`（`main`ブランチ、push済み）
- **フェーズ0**（既存）：`docs/SPEC.md`、`data/requirements/2025-day-common.json`、`data/requirements/2025-day-I-media.json`、`data/subjects/youran-2025.json`、`scripts/gen_data.py`、`scripts/validate_data.py`
- **Vite + React + TypeScript 初期化**：`npm create vite --template react-ts` の雛形からデモコンテンツを除去。`npm run build` / `npm run lint`（oxlint）/ `npx vitest run` すべて通過確認済み
- **フェーズ1**：`src/domain/requirements.ts` を実装（充足判定ロジック）。必修/選択/自由/国際の判定、選択科目の超過分を共通単位に繰り入れる処理、「上級科目」のように親グループ自体が判定境界を持つケースでの二重計上防止、履修中科目の見込み計算（`projected`）に対応。`src/domain/requirements.test.ts` に単体テスト15件（仮想データ＋実データ統合テスト）、全て通過
- **フェーズ2-1**：`react-router-dom` 導入、`HashRouter` で SPEC §6 の9ページぶんのルートを配線（中身は `PagePlaceholder` を使った準備中の空箱）。`src/storage/localStorage.ts` に薄いlocalStorageラッパー（具体的な保存データの形はまだ未定）。Playwrightで9ルートの表示とconsoleエラー無しを確認済み（Playwrightはこの検証のためだけに一時インストールしたもので、プロジェクトの依存には加えていない）
- **科目番号の主キーをフルコード方式に変更**：当初「末尾記号を除いた番号で名寄せ」する設計だったが、類専門科目の選択科目のようにプログラムごとに独自採番されている科目では、末尾を除くと番号が一致しても別科目になるケース（例：`INS502a`＝メディアリテラシー ≠ `INS502b`＝多変量解析）が見つかったため、**末尾記号を含むフルコードをそのまま主キーにする**方式に変更（開発者の承認済み）。`docs/SPEC.md` §7.1、CLAUDE.md本文の規約、`scripts/gen_data.py`・`scripts/validate_data.py`・`src/domain/requirements.ts`のコメントを更新済み
- **Ⅰ類の残り4プログラムのデータ化完了**：経営・社会情報学(b)・情報数理工学(c)・コンピュータサイエンス(d)・デザイン思考データサイエンス(e)。`data/requirements/2025-day-I-{management,mathinfo,cs,designds}.json` を追加。付録C 注1「他プログラムの科目も選択として履修できる（実験科目を除く）」は、各プログラムの類専門（選択）グループの `subjects` に、他の同じ類のプログラムの選択科目コードをデータ生成時にあらかじめ展開して反映（実験科目・自由科目・国際科目は対象外という解釈で実装。判断の詳細は `scripts/gen_data.py` の `build_program`/`cluster_i` 付近のコメント参照）
  - 生成はPythonが使える環境でないため、Node.js版のスクリプト（一時ファイル、リポジトリには含めていない）で実行し、`data/`に直接書き出した。**`scripts/gen_data.py`（正式なPython版）は同じ内容になるよう手で追記したが、この環境ではPythonが無く実行して確認できていない**。次にPythonが使える環境で `python scripts/gen_data.py && python scripts/validate_data.py` を実行し、`git diff`が出ない（＝出力が今のdata/と一致する）ことを確認してほしい
  - 検証は `scripts/validate_data.py` と同じロジックをNode.jsに移植して実行し、必修グループの単位合計・区分の子合計・審査条件の参照先などをチェック済み（OK）。加えて `npx vitest run` / `npm run build` / `npm run lint` も全て通過

### 参考PDF（開発者が用意したもの。リポジトリには含めない）

- `C:\Users\maita\Downloads\uec-credit-route\tanni_extract_final.pdf`（プロジェクト内、`.gitignore`の`*.pdf`で除外済み）：学修要覧2025の本編。第2章（科目区分・単位算出基準・審査所要単位）、別表2〜5、付録C（**全15プログラム＋夜間主課程のカリキュラム表**）を含む、事実上のフル版。Ⅱ・Ⅲ類・夜間主のデータ化にはこれを使う
- `C:\Users\maita\Downloads\youran2025-gakuiki.pdf`（プロジェクト外、`Downloads`直下）：おそらく同じ学修要覧の原本。中身は未確認

- **フェーズ2完了（2-2〜2-6）**：
  - 2-2 `src/domain/recommend.ts`：SPEC §8の推奨スコア(w1〜w7)。先修科目・曜日時限はまだシラバスデータが無いので「無ければ条件なし」として扱う設計。単体テスト11件
  - 2-3 `src/pages/SetupPage.tsx` + `src/storage/profile.ts`：プロフィール設定画面。1年生（非推薦）はプログラムを「未定」に固定
  - 2-4 `src/pages/MainPage.tsx` + `src/storage/records.ts` + `src/components/SubjectStatusSelect.tsx`：メイン画面（合計単位・取得単位・不可の単位・残りの必修・区分別の進捗）。**簡略化**：要覧スケッチの「履修予定チェック」の代わりに、全一覧共通の状態プルダウン（未履修/履修中/修得/不合格）に統一。編集は「更新」ボタンを押すまで確定されない（draft/committed分離）。審査（2年次終了時審査等）の合否表示は未実装
  - 2-5 `src/domain/importers.ts` + `src/pages/DataPage.tsx`：本サイト形式JSON（§7.4）のダウンロード・読み込み（マージ）・全消去。友人アプリ取り込み（§7.5、F-2c）は未対応（Should優先度のため）
  - 2-6 `vite.config.ts`（base設定）+ `.github/workflows/deploy.yml`：GitHub Pages自動デプロイ。**GitHubリポジトリ設定でSettings→Pages→SourceをGitHub Actionsにする作業がまだ残っている**（このファイルだけでは有効にならない）
  - 各ページはPlaywrightで実際にdev serverを起動してブラウザ操作を確認済み（consoleエラーなし）。`src/data/requirementSets.ts` がdata/以下のJSONをアプリから読み込む層（`resolveJsonModule`をtsconfig.app.jsonに追加）
- **GitHub Pages設定を有効化・デプロイ成功を確認済み**：`https://weedy-seadragon.github.io/uec-credit-route/` で稼働中
- **メイン画面のUI改善（フェーズ3着手前の調整）**：トップページに説明文を追加、必修ラベルを「(必修・未修得)」→「(必修)」に簡略化、取得単位・残りの必修を区分ごとに見出し分け、「区分別の進捗」→「選択科目」に改称、ファイルのダウンロード・読み込みをメイン画面のツールバー（表示・更新の隣）に移動、リセットボタン（全記録を未履修に戻す）を追加、履修状態プルダウンから「履修中」を削除（未履修/修得/不合格の3択に）
- **他プログラムの専門科目の明示**：`RequirementSet`/`ProgramDoc` に `programSuffix`（自分のプログラムの科目番号末尾記号）を追加。科目コード末尾がこれと違う科目（付録C注1で選択として履修できる他プログラムの専門科目）には「［他プログラム専門科目］」と表示し、選択科目セクションにその単位が専門科目として扱われる旨の注釈を追加
- **科目一覧に標準年次・学期を表示**：`data/subjects/youran-2025.json` に既にあった `standardYear`/`termType` をMainPageの各一覧（取得単位・不可の単位・残りの必修・選択科目）に表示するようにした（データの追加取得は不要だった）
- **共通単位セクションの実装**：MainPageに「共通単位（n/N単位）」セクションを追加。「あぶれ分」（各区分のoverflow合計、デフォルト展開）と「取得した単位」（`countAs: 'common'`指定の科目、`alwaysCommon`科目）を分けて表示。`BoundaryGroup`に`overflow`/`overflowToCommon`/`countAsCommon`フィールドを追加
- **スマホ対応**：`src/index.css`にメディアクエリではなく素朴なCSS（`body`の余白、`nav a`のタップ領域拡大、`button`/`select`の`min-height`）を追加。Playwrightでスマホビューポート（375×667）でも崩れないことを確認
- **フェーズ3完了：Ⅱ類5プログラム・Ⅲ類5プログラム・夜間主課程のデータ化**（`tanni_extract_final.pdf` 付録C C.3.2/C.3.3/C.5/C.6より）
  - Ⅱ類：セキュリティ情報学(f)・情報通信工学(g)・電子情報学(h)・計測制御システム(i)・先端ロボティクス(j)。計測制御/ロボは「データサイエンス演習」が選択科目になる（day-common.jsonの元々の注記が未実装だった穴を埋め、`commonOverrides`という新しい仕組みで対応：プログラム側から共通ファイルの特定グループのフィールドを部分上書きできる。`src/data/requirementSets.ts`の`applyCommonOverrides`参照）
  - Ⅲ類：機械システム(k)・電子工学(m)・光工学(n)・物理工学(p)・化学生命工学(r)。全プログラムでデータサイエンス演習が選択科目（`commonOverrides`使用）。プログラムによって類共通基礎・類専門の選択タイル構成が違う（選択必修のみ／選択必修+選択の2段／選択必修のみで選択科目自体が別枠、など）ことが分かったので、パターンを一つに決め打ちせずPDFの罫線を都度確認しながら組んだ
  - 夜間主：`data/requirements/2025-evening.json`。**類・プログラムの区分がない単一課程**で、総合文化・実践教育・専門科目とも昼間コースとは別の科目区分・科目コード体系（総合文化/実践教育は's'、専門は't'サフィックス）のため、`2025-day-common.json`をextendsしない自己完結ファイルにした。これに伴い`ProgramDoc.cluster`の型を`null`許容に変更し、`getRequirementSet()`は`course: 'evening'`のとき共通ファイルとのマージをスキップするよう分岐。`SetupPage.tsx`/`MainPage.tsx`もcluster:nullを扱えるよう修正（夜間主コース選択時は学年だけを聞く、プログラム選択欄は出さない）
  - 各プログラムのmajor-sel（類専門・選択）には、付録C注1「同じ類の他プログラムの専門科目も選択科目として算入できる（実験科目を除く）」に対応する他プログラム分の科目コードを展開済み（Ⅱ類・Ⅲ類それぞれ別コミットで、全プログラムがそろってから相互展開する形で実施）
  - 全プログラムで、必修グループの単位合計が別表2・別表4の必修単位と一致することをNode検証スクリプトで確認済み。Playwrightで各プログラムの選択・表示・consoleエラー無しも確認
- **`scripts/gen_data.py` / `scripts/validate_data.py` の更新（Ⅱ類・Ⅲ類・夜間主対応）**：`validate_data.py`は`PROGRAM_FILES`に16ファイル全部を追加し、`reviews`キー（Ⅰ類5プログラムにしか無い）を`.get()`で任意扱いに変更。`gen_data.py`は`add2()`という新しいヘルパーを追加し（既存の`add()`は科目番号の数字から標準学期を自動推定するが、Ⅱ類以降はPDFの週時間数表を実際に読んだ値を明示的に持たせたかったため）、Ⅱ類5・Ⅲ類5・夜間主1の科目マスタ行と要件groups木を追記した。**この環境にはPythonが無く実行して確認できていない**が、生成元のdata/*.jsonをNode.jsで機械的にPythonリテラルへ変換する方式で書いたため、手打ちのtypoリスクは低い。変換の正しさ自体は「生成したPythonリテラルをJSへ逆変換して元のJSONとbyte-for-byte一致するか」「validate_data.pyと同じチェックロジックをNode.jsに移植して16ファイル全部に通す」の2通りで検証済み（どちらもOK）。**次にPythonが使える環境で `python scripts/gen_data.py && python scripts/validate_data.py` を実行し、`git diff`が出ないことを確認してほしい**

- **シラバスから曜日時限を取得（完了、Ⅰ類昼間中心）**：`https://kyoumu.office.uec.ac.jp/syllabus/2025/GakkiIchiran_31_0.html`（情報理工学域2025年度、全1157行）を取得し、`data/subjects/youran-2025.json`の科目名と一致する行（464件）だけに絞り込んで、各行の個別シラバスページ（`.../31/31_{時間割コード}.html`）を1.2秒間隔で取得。科目番号欄（1つの講義が複数プログラムで共有される場合は空白区切りで複数コードが並ぶ。例:`"ELE301a ELE301b ELE301c ELE301d ELE301e"`）を読み取り、一致した科目コード614件分に`offerings`（`docs/SPEC.md` §7.1のスキーマ：`timetableCode`/`faculty`/`term`/`slots`/`instructors`/`syllabusUrl`/`updatedAt`）を追記した。`prerequisites`（先修科目）は今回は未取得（個別ページに項目はあるが今回はスコープ外）
  - 実行はこの環境にPythonが無いためNode.jsで実施し、`data/subjects/youran-2025.json`に直接書き出した。**`scripts/fetch_syllabus.py`（正式なPython版）は同じロジックになるよう新規作成したが、この環境ではPythonが無く実行して確認できていない**
  - **注意：`scripts/gen_data.py`は`offerings`を一切知らない**（PDFの手転記データからしか科目マスタを組み立てないため）。次に`python scripts/gen_data.py`を実行すると`data/subjects/youran-2025.json`が上書きされ、今回取得した`offerings`が消える。`gen_data.py`のあとに`fetch_syllabus.py`を実行する、という順序で運用すること（このルールをスクリプト側で強制してはいないので注意）
  - 464件の候補は「科目名が`data/subjects/youran-2025.json`に存在する行」で絞り込んだもので、全1157件は回っていない（他学域・他年度相当・まだデータ化していない科目名などは対象外）。取りこぼしがある可能性がある
  - `src/domain/recommend.ts`の`SubjectInfo.slots`/`prerequisites`は「科目1つにつき1つの時限」を想定した設計だが、今回取得した`offerings`は科目によって複数セクション（クラス）で曜日時限が違うことがある（例:語学科目で同じ科目コードに6セクション）。**`offerings`をどう`slots`に落とし込むか（先頭のセクションを使う／全セクションを比較して警告するなど）は未決定・未実装**。今回はデータ取得（`data/`への格納）までで、`recommend.ts`側への配線はまだ行っていない

- **学域特別講義（UEC001z・UEC002z）を常時共通単位に分類**：これまで`otherSubjects.special`という未使用の別枠に置いていた（`groups`のどの区分にも属さず、アプリからも一切参照されていなかった）が、開発者の指示（2026-09-05）で`commonCreditSources.alwaysCommon`に移した。理由は元々の科目データの`note`の通り「開講年度により扱いが異なり、必修/選択のどの区分にも一意に割り当てられない」ため。`data/requirements/2025-day-common.json`と`scripts/gen_data.py`を修正済み（`otherSubjects`には`japanese`のみ残した）。これで選択科目の「共通単位」の入れ子（前述）にも表示され、状態プルダウンで選択できるようになった

- **Python導入・`gen_data.py`/`validate_data.py`の実行確認が完了（2026-09-05）**：この環境の`python`/`python3`コマンドはこれまでWindows Storeの偽エイリアス（実体が無い）だったため、`winget install Python.Python.3.12`で本物のPython 3.12.10を導入した（`python`という単純なコマンド名は今も偽エイリアスが優先されるため、フルパス`C:\Users\maita\AppData\Local\Programs\Python\Python312\python.exe`で呼び出す必要がある）。その上で`python scripts/gen_data.py && python scripts/validate_data.py`を初めて実際に実行し、以下を確認した：
  - **`scripts/gen_data.py`に実際にバグがあった**：Ⅰ類5プログラムの類専門（選択）相互展開処理（付録C注1対応、本ファイル内`cluster_i`のループ）で、`for p in cluster_i: ... del p["_majorSelOwn"]`という1つのループの中で読み取りと削除を同時に行っていたため、2プログラム目以降が前のプログラムの`_majorSelOwn`を参照できず`KeyError`で毎回落ちていた（＝これまで一度も最後まで実行できていなかった）。削除を全プログラム分の合算が終わった後の別ループに分離して修正済み
  - 修正後、Ⅰ類5・Ⅱ類5・Ⅲ類5の計15プログラムの要件JSONは**実行結果と`data/`の内容が完全一致**（`git diff`が空）。これまでNode.jsで手作業移植・照合していた内容が正しかったことが実際のPython実行でも裏付けられた
  - `data/requirements/2025-evening.json`だけ128行の差分が出たが、中身（科目コード・順序）は完全一致で、一部の配列を1行にまとめて書いていた箇所をPythonの標準整形（`json.dump(..., indent=2)`、1要素1行）に合わせただけの整形差分。この差分は反映済み（今後は日本主ファイルもPython出力と一致する）
  - `data/subjects/youran-2025.json`は想定通り`gen_data.py`実行で`offerings`が失われたため、`git checkout`で復元済み。**引き続き`gen_data.py`のあとに`fetch_syllabus.py`を実行する運用を守ること**（`fetch_syllabus.py`自体はまだ実行確認していない。長時間かかるシラバス取得が絡むため別タスク）
  - `validate_data.py`は`OK: subjects=1388 programs=16`で正常終了。`npx vitest run`（39件）・`npm run build`も引き続き通過

- **曜日時限表示・クラス入力を実装（2026-09-05）**：`src/pages/MainPage.tsx`に`dayPeriodTag(code)`を追加し、状態プルダウンの右に「木・1限」のように表示するようにした。ただし**セクション（offerings）が1つだけの科目（347件）にしか表示しない**。`src/storage/profile.ts`の`Profile`に`yearOneClass`・`classIABC`・`classIIArea`・`classIIIYear2Class`・`classIIIYear2Area`を追加し、`SetupPage.tsx`でプログラムと同じタイミングで入力できるようにした（類ごとに出し分け、1年次クラス以外はデフォルト「未定」）
  - **重要な制約が判明**：`scripts/fetch_syllabus.py`で取得した`offerings`には、そのセクションが「どのクラス（1年次クラス番号やA/B/Cクラスなど）向けか」という情報が一切含まれていない（timetableCode・担当教員・曜日時限だけ）。そのため、**上記で追加したクラス入力は、今のところ複数セクションがある科目（267件）の曜日時限解決には使えていない**。単一セクションの科目（347件）はクラスに関係なく表示できるので実装済みだが、複数セクションの科目は依然として「未定」時と同じく非表示のまま
  - **解決には時間割PDF（`https://kyoumu.office.uec.ac.jp/timet/A1.pdf`〜`A8.pdf`）の格子（どのクラスの列のどの曜日時限に何の科目・どの教員か）を読み取って、`offerings`の担当教員名と突き合わせる**という追加のデータ作業が必要（教員名が一致する行を探して、そのセル位置の曜日時限をそのクラスの正解とする、という方式が考えられる）。今回はここまでは着手していない

- **`scripts/fetch_syllabus.py`の実行完了（2026-09-05）**：User-Agentの個人メールアドレスを`https://github.com/weedy-seadragon/uec-credit-route`表記に修正した上で実行。`gen_data.py`実行直後（offeringsが無い状態）から実行し、**613科目**（前回614件とほぼ同数）にofferingsを再取得できた。Ⅰ・Ⅱ・Ⅲ類とも取得できている（下記3番参照）。夜間主は今回も0件

### 次回以降にやること

1. **複数セクション科目の曜日時限解決（実装完了、データは拡充中）**：
   - 当初は時間割PDFの格子を1コマずつ書き起こす方式（`class_schedule.csv`）で着手したが手間が大きすぎたため見送った（開発者判断、2026-09-05）
   - その後、**逆方式に転換して解決**：科目名・曜日時限・担当教員は`offerings`から自動で埋め、開発者が「受講対象クラス」（`class_id`）だけを画像認識で埋める方式（`data/timetable/class_assignment.csv`、`scripts/build_class_assignment.py`）に変更。385科目・1343行中、開発者が**775行を記入**（`class_assignment_filled.csv`）
   - `scripts/build_class_assignment_json.py`で`data/timetable/class_assignment.json`に変換し、`src/domain/classAssignment.ts`（`classIdMatchesProfile`・`resolveSlotsForProfile`、単体テスト11件）で「クラスN」「A/B/Cクラス」「INクラス」「Mエリア」「Mエリア(Nクラス)」「プログラム名」という表記ごとにプロフィールと突き合わせる判定ロジックを実装
   - `MainPage.tsx`の`dayPeriodTag`から呼び出し、`class_assignment.json`とプロフィールのクラス情報から一意に決まる場合だけ曜日時限を表示するようにした（決まらない場合は非表示のまま、フォールバック方針通り）
   - **重大なバグの根本原因を特定・修正（2026-09-05）**：開発者が「class_assignment_filled.csvのコンピュータリテラシーが時間割PDFと合っていない（若月先生の枠が違う）」と報告。調べたところ、**`fetch_syllabus.py`がシラバスの2025年度分（`.../syllabus/2025/...`）を見ていたことが原因**と判明。`data/subjects/youran-2025.json`の「2025」は学修要覧の入学年度（カリキュラムの版）であって、シラバスを見るべき開講年度とは別物。時間割PDFは2026年度のものなので、シラバスも2026年度分（`.../syllabus/2026/...`）を見る必要があった。2025年度と2026年度で同じ科目でも担当教員の曜日時限が変わっており（若月先生と八木先生の枠が2025→2026で入れ替わっていた等）、これが「入れ替わりバグ」に見えていた原因。`fetch_syllabus.py`に`SYLLABUS_YEAR`定数（現在2026）を追加して再取得。合わせて`build_class_assignment.py`のマージキーを「科目・学期・曜日・時限」（教員名を含めない）に変更し、シラバス年度切り替えで担当教員が変わっても記入済みclass_idが引き継がれるようにした。この修正により、コンピュータリテラシーの2件は自動解決に変わった（クラス7・クラス2、PDFと一致）
   - **`class_assignment_filled.csv`の残り`unresolved`16件は正当な未解決と確認済み**：全て独語・仏語・韓国朝鮮語の第一/第二（第二外国語選択科目）で、「学生がどの言語を選ぶかで決まり、特定のクラス番号には紐付かない」という構造上の理由。データの不具合ではない
   - **UI設計について開発者と相談済み（2026-09-05、まだ実装はしていない）**：曜日時限の表示は科目名の左ではなく、未履修等の状態プルダウンの右側に置く方向。表示対象はⅠ類昼間だけでなく全プログラム分（＝下記のシラバス取得対象拡大が前提）。英語（Academic English等）はクラス分けを聞かずに「1-3限」のような時限レンジ表示で済ませる方針
   - **クラス分けの実態を`https://kyoumu.office.uec.ac.jp/timet/A2.pdf`・`A3.pdf`・`A4.pdf`（2026年度昼間コース時間割）で確認済み（2026-09-05）。当初の想定（1年次クラス1〜12・2年次クラスA〜Cを聞けばよい）より複雑だった**：
     - `A2.pdf`＝1年次（前学期・後学期共通）：クラス1〜12は類に直結（クラス1〜4＝Ⅰ類、5〜8＝Ⅱ類、9〜12＝Ⅲ類）。この番号は学籍番号による割り当てで本人の選択ではないため、**ユーザーに直接入力してもらう以外に決める方法が無い**。なおⅠ類の第二外国語等一部科目だけ別に「Aクラス/Bクラス/Cクラス」（クラス1・2の一部／クラス2・3の一部／クラス3の一部・4、のように学籍番号下三桁で機械的に決まる）が存在し、A2上のクラス1〜12とは別軸
     - `A3.pdf`＝2年次**前学期（第3学期）のみ**：Ⅰ類はA/B/Cクラス、Ⅱ類はI1〜I6クラス＋Mエリア、Ⅲ類は1〜4クラス。これらは「1年次のクラス番号」×「学籍番号下三桁」の組み合わせで機械的に決まる資料上の割り当てであり、本人が選ぶものではない。逆算するより「2年次のクラスを直接入力してもらう」方が現実的（対象は前学期だけなので、後学期は下記の通り不要になる）
     - `A4.pdf`＝2年次**後学期（第4学期）以降**：列がクラスではなく**プログラム名**（メディア情報学、経営・社会情報学…）になる。つまりこの学期以降はプロフィールの「プログラム」情報だけで大半の科目の時限が特定できる。ただし一部科目（例:Ⅲ類電子工学プログラムの基礎電子回路）はプログラム内でさらに「クラスA/B/C/D」に分かれており、この粒度の科目だけ追加のクラス入力が要る
     - 3・4年次のクラス分けの資料は未確認（同様の`timet/`配下にある可能性が高い。次回確認する）
     - **示唆**：「1年次クラス」「2年次前学期クラス（前学期のみ有効）」を素直にユーザー入力してもらい、プログラム確定後（2年次後学期以降）はプログラムで足りる科目が大半、という設計の方が「1年次クラスから2年次クラスを逆算する」より単純で頑健そう。ただし一部科目のプログラム内サブクラスなど、完全に取りこぼしを無くすには3・4年次分の資料確認が必要
     - **環境メモ**：この環境にはPDFをページ画像化する`pdftoppm`（poppler）が無かったため`winget install oschwartz10612.Poppler`で導入した。Readツール自体はpoppler未導入時と同じ理由（PATHが同一プロセス内で更新されない）でPDFを直接開けなかったため、`pdftoppm -png`で画像化してからReadツールで画像として読む、という手順を踏んだ
   - **入力仕様が確定（2026-09-05、まだ実装はしていない）**。プロフィール設定で「プログラム」を聞くのと同じタイミングで、以下をあわせて聞く：
     1. **1年次クラス**（全員に聞く。類に応じてⅠ類=1〜4／Ⅱ類=5〜8／Ⅲ類=9〜12の範囲から選ばせる。学籍番号による機械的な割り当てで本人の選択ではないため、他から逆算はできず本人に直接答えてもらうしかない）
        - **学籍番号の偶奇はここから導出できる（開発者指摘、2026-09-05）**：「学籍番号が奇数ならクラスも奇数、そうでなければ偶数」という対応関係があるため、学籍番号そのものは別途聞かず、1年次クラスの数字の偶奇をそのまま学籍番号の偶奇として判定・保存しておく。3年次以降に出てくる「学籍番号偶数クラス／奇数クラス」区分の科目（A6等で確認済み）の判定に使う
     2. **1年後期〜2年後期クラス（Ⅰ類のみ表示、A/Bクラス/Cクラス）**：デフォルト「未定」
     3. **2年前期クラス/エリア（Ⅱ類のみ表示、I-1〜I-6クラスまたはMエリア）**：デフォルト「未定」
     4. **2年前期クラス（Ⅲ類のみ表示、1〜4クラス）**：デフォルト「未定」
     5. **2年後期エリア（Ⅲ類のみ表示、M/Sエリア）**：デフォルト「未定」
     - 1年次クラス以外（2〜5）はデフォルトすべて「未定」。3・4年次分の項目は上記の通り資料未確認のため保留
     - **フォールバック方針**：これらが「未定」のまま（＝ちゃんと入力されなかった）場合、英語（Academic English等、時限レンジ表示で済ませる科目）以外で複数の時限候補がある科目は、曜日時限を一切表示しない（誤った1つを適当に選んで出すことはしない）
   - **`https://kyoumu.office.uec.ac.jp/timet/A1.pdf`〜`A8.pdf`（第1〜8学期、全学期分）を追加確認（2026-09-05）**。A1(1年前期)〜A8(4年後期)の全体像：A1・A2（1年次）はクラス1〜12、A3（2年前期）はⅠ類=A/B/Cクラス・Ⅱ類=I1〜I6クラス+Mエリア・Ⅲ類=1〜4クラス、A4（2年後期）以降は列がプログラム名になる（Ⅲ類はMエリア/Sエリアの二段構え）。A5〜A8（3年前期〜4年後期）はプログラム名の列のみで、クラス・エリアの概念自体が無い（一部科目に学籍番号奇数/偶数などのサブクラスがあるのみ＝科目単位の話で、プロフィールの入力項目としては不要）
     - **重要な訂正（開発者指摘、2026-09-05）**：A4（2年後期）はほとんどの科目がプログラム列だが、**Ⅰ類の「アルゴリズム論第一」はA4でも列がA/B/Cクラスごとに分かれている**。つまりⅠ類のA/B/Cクラスは2年後期でも一部科目に効いてくるため、上記2番の「1年後期〜2年後期クラス」という範囲は**そのままで正しい（訂正しない）**
     - 3・4年次（A5〜A8）はクラス・エリアの入力が一切不要と判明したので、上記「3・4年次分の項目は保留」は解消（＝追加のプロフィール入力項目は無い）
     - **注意（開発者への報告）**：この3ファイルをダウンロードする際、外部サイトへのUser-Agentの連絡先部分に開発者の個人メールアドレスをそのまま使ってしまうミスがあった（開発者からの指摘で気づき、以後は個人メールを使わない方針に修正済み）
2. **シラバス取得の対象拡大（続き）**：調査した結果、当初の認識を訂正する必要があった（2026-09-05）：
   - **実はⅠ類だけでなくⅡ・Ⅲ類も既にそれなりの件数がofferings取得済みだった**（名前一致による絞り込みなので、Ⅱ・Ⅲ類データが揃った後に実行すれば自然と拾われる）。前回実行時点の内訳：Ⅰ類各プログラム38〜45件、Ⅱ類各プログラム16〜29件、Ⅲ類各プログラム18〜38件、共通(z)133件。「Ⅰ類昼間中心」という当初の記述は不正確だった
   - **夜間主のシラバスは学域コード32で見つかった（2026-09-05、解決済み）**：開発者の指摘で「学修要覧に載っているのだからシラバスも本当に無いのか」を確認したところ、`GakkiIchiran_32_0.html`（学域コード32）が夜間主課程のシラバス一覧だった（83行中69行が夜間主科目名と一致）。`31_1.html`・`31_2.html`は結局未確認のままだが、少なくとも夜間主は32で解決した。`scripts/fetch_syllabus.py`を`FACULTIES = ["31", "32"]`で両方まわるよう修正して実行し、夜間主68科目分のofferingsを取得済み（合計687科目）
   - **重大なマッチング漏れバグを発見・修正（2026-09-05、開発者がTestCase1でAcademic Written English Ⅰの表示異常を報告して発覚）**：語学・実験科目のように1科目が多クラスに分かれている場合、シラバス一覧の科目名には「Academic Written EnglishⅠ（金1・F）」のようにクラス表記が付くが、科目マスタの名前にはこの表記が無いため完全一致せず、**多クラス科目のほぼ全クラスがこれまで1件も取得できていなかった**。そのうえ、たまたま名前が完全一致する無関係な1件（このケースでは夜間主側の1クラス）だけが「唯一のクラス」として誤表示される事故が起きていた。`scripts/fetch_syllabus.py`に`strip_class_suffix()`（末尾の（…）を取り除いて再突き合わせ）を追加して修正し、再実行。offerings付与件数は687→**1046科目**に増加（単一セクション660件・複数セクション386件）
   - **ENG101s/ENG101zの科目番号入れ替わりを発見・補正**：上記の修正後も判明した個別の不具合。シラバスシステム上で、夜間主の「Academic Written English I」(ENG101s)の欄に昼間の金曜多クラス分26件（学修要覧の昼間担当教員陣と一致）が、逆に昼間の「Academic Written EnglishⅠ」(ENG101z)の欄に夜間主の1クラス分（土曜、Dusza/Jeffreys担当）が登録されていた。他の夜間主科目にはこの現象は無く、この2科目だけの大学側の登録ミスと判断し、`fetch_syllabus.py`に`CODE_SWAP_FIXUPS`（明示的な入れ替え補正リスト）を追加して対処
   - **`prerequisites`（先修科目）はテキストのまま`prerequisitesText`として取得済み（2026-09-05）**：シラバス個別ページの「前もって履修しておくべき科目」欄は自由記述のテキストなので、科目コード配列への変換（誤検出リスクが高い）はせず、テキストをそのまま`SubjectMasterEntry.prerequisitesText`に保存するだけにとどめた。885科目分取得。`recommend.ts`の`SubjectInfo.prerequisites`（コード配列）への変換・配線はまだ行っていない
   - **`31_1.html`・`31_2.html`は調査済み・対応不要と判明（2026-09-05）**：`31_0.html`と同じ学域31（昼間）のページで、timetableCodeが2つとも`31_0`の完全な部分集合（新規科目コード0件）だった。既存の`31_0`だけの取得で全件カバーできている
   - **文字化けに関する注意**：この作業中、シラバス一覧ページのHTMLを一時的に「文字化けしている」と誤認し、進行中の`fetch_syllabus.py`を一度誤って停止してしまった。実際には文字化けはこのBashツール（Windows/MSYS環境）でPythonの`print()`結果を表示する際の見た目上の問題だけで、実データ（UTF-8で`decode`した文字列そのもの、および`json.dump(ensure_ascii=False)`で書き出すJSON）は正しい。日本語を含むPythonの`print()`出力をこの環境で目視確認する際は、文字化けして見えても実データの破損と早合点しないこと（`PYTHONIOENCODING=utf-8`を設定しても改善しない場合がある＝表示側の問題である可能性が高い）
3. **審査（2年次終了時審査等）の合否表示を実装完了（2026-09-06）**：`src/domain/reviews.ts`に`allOf`/`anyOf`の条件木評価ロジックを実装（`groupMin`・`allPassed`・`subjects`・`totalCredits`・`commonCredits`・`allGroups`・`review`の全条件タイプに対応）。単体テスト11件（仮想データ＋Ⅰ類メディア情報学の実データ統合テスト、実際に必要科目を合格にしてy2-end審査だけ合格に変わることまで確認）。`MainPage.tsx`に「審査」セクションを追加し、`reviews`データがあるプログラム（今のところⅠ類5プログラムのみ）で表示。不合格時は詳細を展開して不足条件を文章で表示。Playwrightで実ブラウザ表示も確認済み（この環境はPlaywright独自のダウンローダーがなぜかタイムアウトするため、Chromiumを直接ダウンロード＆手動配置する必要があった）。Ⅱ・Ⅲ類・夜間主はreviewsデータ自体が無いので、それらのプログラムでは審査セクション自体が表示されない（今後データを追加すれば自動的に表示されるようになる設計）
4. 今回スコープ外にした主な項目：F-2c（友人アプリ取り込み）・F-2d（成績表貼り付け）、同時限警告（上記1番の設計判断待ち）、F-5（科目一覧・詳細）、F-6（プログラム比較）、F-9のOGP/SEO/sitemap（SPEC上もフェーズ4の予定）、夜間主の国際科目（付録C C.4、3・4年次のみ・上級科目扱い）の組み込み
5. **Ⅱ・Ⅲ類・夜間主の審査データ作成：着手したが中断（2026-09-06、開発者判断で時間割データ作業を優先するため一旦保留）**。`reviews.ts`のロジック自体は実装済みなので、データさえ用意すれば表示は自動的に有効になる。以下、次回再開のための調査結果：
   - **出典ページを特定済み**（`PDF/tanni_extract_final.pdf`、全54ページ）：
     - `別表3`「2年次終了時審査（昼間コース）のための授業科目」＝ページ6。総合文化8単位・実践教育8単位・専門科目21単位(Ⅰ類)/24単位(Ⅱ類)/23単位(Ⅲ類)というカテゴリ単位の基準は全プログラム共通。ただし**Ⅱ類だけ追加で「2年次開講の確率統計」の修得も必要**という注記あり
     - `別表3の2`「2年次終了時審査不合格の場合、履修できない3年次以降の授業科目」＝ページ7。15プログラム全部の対象科目名を記載（`onFail.blockedSubjects`用）。科目名→コードへの変換がまだ
     - `別表4`「卒業研究着手審査基準」＝ページ9〜10。15プログラム全部の詳細な単位数・科目要件を記載
   - **重大な発見：Ⅰ類で使った条件タイプだけでは足りない**。`ReviewCondition`のスキーマ拡張が必要：
     - 「全必修10科目のうち9科目以上を修得」（情報通信工学・電子情報学の類共通基礎）のような**科目の本数**による条件（単位数の`groupMin`とは別物）。新しい条件タイプ（例:`requiredCountMin`）が必要
     - 「2年次までの類共通基礎及び類専門の必修9科目21単位のうち16単位以上」（機械システム等Ⅲ類5プログラム共通のパターン）のような、**単一グループでなく複数グループをまたいだ単位数のしきい値**。既存の`groupMin`は単一グループのidしか受け取れないので、複数グループの科目を合算する条件タイプ（例:`groupsMin`で`groupIds`配列を受け取る、または`subjects`条件を単位数しきい値版に拡張した`subjectsMin`）が必要
   - **示唆**：単位数を勝手に近似すると誤った合否判定になりかねないので、スキーマ拡張後、別表4を1行ずつ正確に書き起こす必要がある。プログラムごとにパターンが違う（セキュリティ情報学は比較的単純、情報通信工学/電子情報学は科目本数条件、計測制御/先端ロボティクスはallPassed+groupMinの組み合わせで書けそう、Ⅲ類5プログラムは複数グループ合算パターン）ので、パターンごとにグルーピングして進めると効率が良さそう
   - 夜間主に同様の審査制度があるかどうかは未調査

6. **`class_assignment_filled.csv`完成（開発者が全1266行を手作業で記入）を点検し、全データを反映（2026-09-06）**：開発者から「学年・年次の不整合」「必修同士の同時限重複」「集中講義の誤判定」「その他なんでも」の4点チェックを依頼され、就寝中に自律実行。結論：**除外すべき異常は無く、全データを反映した**。ただし点検の過程で以下のコード修正・データ正規化を行った：
   - **`classIdMatchesProfile`に新しい表記を追加**：点検中に未対応の`class_id`表記（`全クラス`・`Iエリア`・`二類学籍番号偶数/奇数`・`留学生`・`再履全員`・`再履生`）が見つかった。**`全クラス`（誰でも一致）・`Iエリア`（Ⅱ類でclassIIAreaがMエリア以外＝I1〜I6のいずれか）・`二類学籍番号偶数/奇数`（Ⅱ類で1年次クラス番号の偶奇と一致するかで判定。1年次クラスの偶奇＝学籍番号の偶奇という開発者指摘済みの対応関係をここでも利用。2026-09-06、開発者からの再指摘で対応漏れに気付き追加）は既存のプロフィール項目だけで判定できるので実装した**。残り2つ（留学生・再履）は対応する情報をプロフィールが持っていないため、新しいプロフィール入力項目を追加するかどうかの設計判断が要る。開発者の判断を仰がずに項目を増やすのは避け、**今は未対応のまま（`classIdMatchesProfile`は該当表記に対してfalseを返す＝その科目の曜日時限は表示されないだけで、誤表示にはならない）**にした。実例：`FGN102*`（基礎物理学演習第一、留学生向け）、`MCE401k`/`MCE403k`（材料力学/機械力学および演習、機械システムプログラム内で学籍番号により分かれる。**CSVの備考欄に開発者自身が「学籍番号により分割されるがclass_idでは表現できないため機械システムプログラムとだけ記入」と明記済み**で、データ不備ではなく既知の制約）
   - **`resolveSlotsForProfile`のロジック修正**：複数セクションが一致した場合に無条件でundefined（非表示）を返していたが、**一致した全セクションの曜日時限が完全に同一なら、担当教員までは分からなくてもその曜日時限を返すように修正**した。第二外国語（独語第一等）で「同じクラス向けの科目が教員違いで複数開講されているが同じ曜日時限」というケースが実際にあり、この修正で正しく表示できるようになった（曜日時限が食い違う場合は従来通りundefined）。単体テスト2件追加
   - **データの正規化（1点のみ）**：中国語第一/第二・仏語第一/第二・独語第一/第二・韓国朝鮮語第一/第二（計8科目・第二外国語グループ）の`class_id`だけ、他の全箇所と表記順が逆（`1クラス`のように数字が先）になっていた。意味は`クラス1`と同じで曖昧さは無いため、他の箇所と同じ`クラスN`の順に正規化した（40行）。同じ学生が「1年次クラス1〜12のどれかを選んで言語を決める」という開発者が発見した仕組み（全言語が同じクラス群で同じ曜日時限になる）に基づく入力で、意味自体に誤りは無かった
   - **必修同士の同時限重複チェック**：単純な「同じ曜日時限に複数の必修科目コードがある」だけでは、他プログラム名のトークンが多数該当してしまい誤検出だらけになったため、「学生自身のプログラムに実際に一致するトークンだけ」に絞って再検証。結果、上記のMCE401k/MCE403k（学籍番号分割、既知の制約）以外に真の重複は無かった
   - **集中講義判定チェック**：`youran-2025.json`で「offeringsはあるがslotsが全部空」の科目が140件あり（卒業研究・輪講・工房・インターンシップ等の性質上妥当なものが大半）、その中で本当に集中講義か疑わしい2件（`UEC501z`データサイエンス演習・`CHM502r`有機化学第一）のシラバスページを実際に取得して確認したところ、**どちらも本当に曜日時限の記載が無かった**（シラバス側のデータが元々そうなっている。取得側のバグではない）。140件全部を洗い出したわけではないが、抽出方式自体に誤りは見られなかった
   - **学年・年次の不一致チェック**：当初46件の「標準年次と食い違う`class_id`」を検出したが、これは検証スクリプト自身の誤り（`クラスN`のクラス分けがⅠ類の1年次だけでなく2年次の科目にも、`I1〜I6クラス`/`Mエリア`のクラス分けが2年次だけでなく3年次の科目にも、実際に再利用されている）と判明。`classIdMatchesProfile`はプロフィールのフィールド値と表記だけで判定しており`standardYear`を一切参照しないため、これらは**アプリの動作に影響しない誤検出**であり、真の異常は無かった
   - `scripts/build_class_assignment_json.py`で`data/timetable/class_assignment.json`を最終版（1266件）に再生成。`npx tsc --noEmit` / `npx vitest run`（66件）/ `npm run lint` / `npm run build` / `python scripts/validate_data.py`すべて通過を確認
   - **開発者への申し送り**：残る`留学生`・`再履全員`・`再履生`の3表記は、対応するプロフィール項目（留学生かどうか、再履修かどうか）を新設するかどうかの設計判断待ち。追加すれば該当科目でも曜日時限が表示できるようになる

7. **「再履生/再履全員」の曜日時限表示に対応（2026-09-06）**：開発者から「不可の科目の下に再履用の科目も書くのはどうか」という提案があった。調査の結果、**再履セクションは別の科目コードではなく、通常セクションと全く同じ科目コードの別セクション（時限違い）として既にデータに入っている**ことを確認（例：`COM401f`は通常セクション「Iエリア」向け（金1・金2）と、再履セクション「再履生」向け（金5・金6）を同じコードで持つ。`data/subjects/youran-2025.json`にも「(再履)」という名前だけの別科目コードは存在しない）。そのため「別科目として扱い、合格したら両方を取得済みに移動、単位は二重計上しないよう0にする」という当初案は不要だった：**同じ1つの科目コードの状態を不合格→修得に変えるだけで、既存の3択プルダウンがそのまま正しく動く（単位の二重計上はそもそも起こらない）**。
   - 代わりに実装したのは、**不合格（再履修中）の科目については、通常セクションではなく再履セクションの曜日時限を表示する**という部分：`src/domain/classAssignment.ts`の`classIdMatchesProfile`・`resolveSlotsForProfile`に`isRetaking`引数（デフォルトfalse）を追加。trueのときは「再履全員/再履生」表記だけが一致し、通常のクラス・プログラム向け表記とは一致しなくなる（同じ科目で通常・再履の両方が一致すると曜日時限が食い違って決められなくなるため）
   - `MainPage.tsx`の`dayPeriodTag`が`committed.get(code) === 'failed'`かどうかを見て自動的に`isRetaking`を決めるようにした（新しいプロフィール項目は不要。「不可の単位」一覧に出ている科目は自動的にこの判定になる）
   - 単体テスト4件追加（`isRetaking`の基本動作、通常セクションとの排他性、`COM401f`を模した実データ相当のシナリオ）。`npx tsc --noEmit` / `npx vitest run`（69件）/ `npm run lint` / `npm run build`すべて通過

8. **「留学生」対応は「転類生」（他の類へ転類した学生）と合わせて後で対処する方針に（開発者判断、2026-09-06）**。転類生は今のところ`class_assignment_filled.csv`に実例が無い（0件）が、留学生と同様「対応する情報をプロフィールが持っていない」という同じ理由で保留になる見込みの類似ケースとして開発者が挙げた。両方ともプロフィール項目を新設するかどうかの設計判断が要るため、まとめて次回以降の課題とする（`再履全員`/`再履生`は解決済み。CLAUDE.md本文の6・7番参照）

9-11（生涯スポーツ演習等の曜日時限バグ修正・ENG302z修正）は上の進捗ログ参照。

12. **Ⅱ類5・Ⅲ類5・夜間主の審査（2年次終了時審査・卒業研究着手審査・卒業審査）データを追加（2026-09-06、開発者が就寝中に自律実行）**：
    - **`tanni_extract_final.pdf`が手元から無くなっていた**（削除されたか移動されたと思われる）。代わりに`C:\Users\maita\Downloads\youran2025-gakuiki.pdf`（225ページ、大学全体の学修要覧原本）を使用。情報理工学域の該当章は物理ページ20〜24（別表3=p20、別表3の2=p21、別表4 4.1昼間コース=p23、4.2夜間主コース=p24）。`pdftotext`はこのPDFでも日本語グリフが空白になる（既知の制約）ため、`pdftoppm`で画像化してReadツールで目視確認する方式で読んだ。**このPDFも引き続きリポジトリには含めない**
    - **スキーマ拡張**：`src/domain/requirements.ts`の`ReviewCondition`に2つの条件タイプを追加。`subjectsCountMin`（指定した科目のうち何科目合格したかを数える。単位数ではなく科目数で判定。例:「全必修10科目のうち9科目以上」）、`subjectsCreditMin`（指定した科目のうち合格済みの単位数を合計してmin以上か。既存の`groupMin`は単一グループのcontributionしか見れないため、複数グループ（類共通基礎科目＋類専門科目など）にまたがる単位数条件に使う。例:「2年次までの必修9科目21単位のうち16単位以上」）。`ReviewDef`には`caveat`（合否に関わらず常に表示する注記。例:「会議の了承を必要とする」。既存の`onFail.note`は不合格時にしか出ないため別物として追加）も追加。`src/domain/reviews.ts`の`evaluateReviews()`に`subjectCredits`引数を追加（`subjectsCreditMin`の単位数計算に必要なため）。`MainPage.tsx`の`describeCondition`・審査セクション表示を対応させた
    - **別表4の値の照合**：全プログラムで「別表4に書かれている単位数・科目数」と「実際にそのプログラムのJSONに入っている該当科目の単位を手計算した合計」が寸分違わず一致することを確認しながら書き起こした（例: 情報通信工学の類共通基礎科目条件「必修10科目のうち9科目、必修+選択必修21単位」→`cluster-basic-req`(10科目20単位)+`cluster-basic-elecreq`(3科目4単位)の組み合わせで21単位が成立、等）。Ⅲ類5プログラムの「2年次までの類共通基礎科目及び類専門科目の必修科目◯科目◯単位のうち◯単位以上」という区分をまたぐ条件は、`cluster-basic-req`の全科目＋類専門科目のうち号番号が若い（`401`/`402`/`403`等）2〜3科目を具体的な科目コード列挙で表現し、単位合計が別表4の数値と一致することを確認済み
    - **`scripts/gen_data.py`にも反映**：Ⅰ類のときと同じ理由（`gen_data.py`実行で`data/`が上書きされるため）で、追加した`reviews`をJSON側からPythonリテラル（`repr()`で機械変換、手打ちなし）に変換して対応する各プログラムの辞書リテラルに追記した。`python scripts/gen_data.py`を実際に実行し、出力が既存の`data/requirements/*.json`と完全一致（純粋な追加のみのdiff）することを確認済み（`data/subjects/youran-2025.json`はいつも通り`offerings`が消えるため`git checkout`で復元）
    - 単体テスト7件追加（`subjectsCountMin`・`subjectsCreditMin`・`caveat`の基本動作、実データでの統合テスト：Ⅱ類情報通信工学プログラム2件・Ⅲ類機械システムプログラム2件）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（78件）・`npm run lint`・`npm run build`すべて通過
    - **UIでのブラウザ確認はしていない**（この環境でのPlaywright起動に past に手間がかかった経緯があり、今回は時間の都合でスキップ。ただし実データを読み込んでの単体テストは通っているので、ロジック自体は検証済み。次回起動して見た目を確認するとより安心）
    - 夜間主は「2年次終了時審査」に相当するものが無い（類・プログラム配属という概念自体がないため）。代わりに2.4.2「輪講履修条件」（3年次前学期終了までに71単位以上、`id: seminar-eligibility`）と、2.4.3「卒業研究着手審査（卒業研究を選択履修する場合）」（`caveat`で「先端工学基礎課程長（会議）の了承を必要とする」旨を表示）を実装

13. **`prerequisites`（先修科目）をrecommend.tsのスコアリングに配線（2026-09-06、開発者が就寝中に自律実行）**：
    - `prerequisitesText`（自由記述）の実データを867件サンプリングして確認したところ、「微分積分学や線形代数学などの数学科目」のような曖昧な言い回し、「特になし」「特にありません」「None」といった否定、英語の文章、「力学第一、力学第二、電磁気学第一、微分積分学第一、第二」のような省略形など、機械的に科目コード配列へ断定するには誤検出のリスクが高い形式が大量にあることを再確認した（過去のCLAUDE.mdの判断が正しかったことの裏付け）
    - そこで**「区切り文字で分けた1トークンが、実在する科目名と完全一致する場合だけ採用する」という保守的な変換ロジック**を`src/domain/prerequisites.ts`に新規実装（`buildNameToCodes`・`derivePrerequisites`）。部分一致・曖昧な言い回しは一切拾わない（見逃し＝先修条件なし扱いになるだけで無害だが、誤って別の科目を要求する方は避ける、という考え方）。同じ科目名が複数プログラムにまたがる場合（例:「力学演習」がⅢ類5プログラム分ある）は、対象科目と同じ末尾記号（プログラム）を優先し、無ければ夜間主以外は共通科目（末尾z）にフォールバックし、それでも決まらなければ諦める
    - **`recommend.ts`のW5（先修科目条件クリア）はこれまで`prerequisites`が誰にも配線されていなかったため、常に無条件でボーナスが付く（＝実質何もしていない）状態だったことが判明**。`MainPage.tsx`の`recommendSubjects`構築時に`derivePrerequisites`を呼んで実際に配線した
    - 実データ867件中418件（約48%）で1つ以上の先修科目コードを抽出できることを確認。抽出結果を目視でも確認し、明らかな誤検出は見当たらなかった（例:「生物化学」→「分子生物学」、「中国語第二」→「中国語第一」等、いずれも妥当）
    - 単体テスト11件追加（合成データでの基本動作・曖昧ケース・プログラムまたぎの優先順位・実データでの全件無エラー実行確認・具体的な実データ2件の期待値確認）。`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過
    - この機能は`recommend()`のスコアに小さな加点（W5=10点）を与えるだけの副次的なランキング要因であり、卒業要件の充足判定（`evaluateRequirements`）には一切影響しない。多少の見逃し（先修科目を拾えないケース）があっても実害は無い設計になっている

14. **`offerings`が取れない科目の名前不一致調査・修正（2026-09-06、開発者が就寝中に自律実行）**：offeringsが1件も無い360科目のうち、英語・日本語・第二外国語系を中心に、シラバス一覧の実際の表記を確認したところ、以下4種類の表記ゆれで名前が一致していなかったことが判明：
    - **スペースの有無**：「Academic Spoken English Ⅰ」（シラバス、スペース有り）vs「Academic Spoken EnglishⅠ」（科目マスタ、スペース無し）
    - **ローマ数字とASCII文字の違い**：夜間主の「Academic English for the 2nd Year Ⅰ」（シラバス、Unicodeローマ数字Ⅰ）vs科目マスタの「...Year I」（ASCII大文字I）
    - **ダッシュの字体違い**：「Technical English － Basic English for Science」（シラバス、全角ハイフンマイナス＋前後スペース）vs科目マスタの「Technical English – Basic English for Science」（エンダッシュ、スペース無し）
    - **末尾の（…）が2つ連続**：「日本語第一（１年）（月２・木２）」のように、既存の`strip_class_suffix`（末尾の（…）を1回だけ除去）では取りきれないケース
    - `scripts/fetch_syllabus.py`に`normalize_for_match()`（空白除去・ローマ数字→ASCII変換・ダッシュ類の統一）を追加し、候補行の絞り込みで正規化した名前どうしでも比較するようにした。`strip_class_suffix()`も末尾の（…）が無くなるまで繰り返し除去するよう修正。**科目マスタ側の`name`フィールド自体（学修要覧の表記）は変更していない**（マッチング専用の正規化なので、表示に使う名前はそのまま）
    - 再実行の結果、**31科目が新たにoffersingsを獲得**（`ENG102s/z`・`ENG201s/z`・`ENG202s/z`・`ENG301s/z`・`ENG401s/z`（英語基礎・第2学年英語）、`JPN101z`〜`JPN301z`（日本語第一〜第三）、`CHI/FRE/GER/KOR/RUS 501z/601z`（外国語とその運用A/B）、`TEN501z`・`TEN601z`（Technical English）、`INT003c/d/g/h`・`INT004z`・`INT005z`）。既存の1028科目からの regression（offeringsを失った科目）は0件と確認済み
    - 残る約330科目（大学院連携科目の大半・卒業研究/輪講など、および原因未特定の少数）は今回のスコープ外。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過

15. **1年次の英語（Academic Spoken/Written English Ⅰ・Ⅱ）が土曜表示になる不具合を修正（2026-09-06、開発者が「1年に取る英語系の時限がおかしい。なんで土曜になってるの？」と報告して発覚）**：`ENG101s`/`ENG101z`（Academic Written English I、夜間主/昼間ペア）は2026-09-05に「シラバスWeb公開システム側で科目番号欄の記載が入れ替わっている」不具合として発見・`CODE_SWAP_FIXUPS`で補正済みだったが、**同じ入れ替わりが`ENG102s/z`（Academic Spoken English I）・`ENG201s/z`（同Written II）・`ENG202s/z`（同Spoken II）の3ペアにも存在し、これらは補正対象に入っていなかった**ことが判明。夜間主の1クラス分（土曜、Dusza/Jeffreys等担当）のofferingsが昼間側（zサフィックス）に、昼間の多クラス分（平日、25〜26件）のofferingsが夜間主側（sサフィックス）に、それぞれ誤って登録されていた。昼間側のこれらの科目はofferingsが（本来複数あるはずなのに）1件しか無い状態になっていたため、`MainPage.tsx`の`dayPeriodTag`が「セクションが1つだけの科目は無条件表示」のルールに従って、その1件（実際は夜間主の土曜枠）をそのまま表示してしまっていた
    - `scripts/fetch_syllabus.py`の`CODE_SWAP_FIXUPS`に上記3ペアを追加。`data/subjects/youran-2025.json`にも同じ入れ替えを直接適用（`gen_data.py`実行のたびにofferingsが消えるのと同じ理由で、次に`fetch_syllabus.py`を実行すれば同じ結果になるよう先にスクリプト側を直し、今のデータには直接パッチした）
    - 修正後は`ENG102z`/`ENG201z`/`ENG202z`とも本来の25〜26件のofferingsを持つようになったが、**英語系科目はそもそも`scripts/build_class_assignment.py`の対象外**（`if "English" in s["name"]: continue`、CLAUDE.mdの「英語はクラス分けを聞かずに時限レンジ表示で済ませる方針」に基づく既存の設計）なので、`class_assignment.json`には対応データが無く、`resolveSlotsForProfile`は一致するセクションを見つけられず`undefined`を返す。結果、修正後はこの4科目とも曜日時限が**非表示**になる（誤った土曜表示より安全）。Playwrightで実際に確認済み（Ⅰ類3年生メディア情報学プログラムで、英語（1年）の4科目とも時限タグが出ないことを確認、consoleエラー無し）
    - 「英語は時限レンジ表示にする」という当初案（1番）は、実際には実装されず**別の方式に置き換わって解決済み**：必修の1・2年次英語（ENG101z〜401z）は19番でクラス別解決の対象に追加され、それ以外の多クラス英語科目（ENG302z等）は10番の「全クラスしか候補が無い場合は全時限を列挙する」フォールバックで対応済み。「1-3限」のような範囲表示は最終的にコード上どこにも実装されていない（2026-09-07、開発者からの指摘で古い記述と気づき訂正）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過

16. **「基礎科学実験A1/A2/B1/B2」の科目名の全角英数字を半角に修正し、offeringsを新規取得（2026-09-06、開発者が「基礎科学実験系の英語と数字は半角で、あとこいつらの時限なんで表示されないの？」と報告して発覚）**：`PHY101z`（基礎科学実験A1）・`PHY201z`（同A2）・`CHM101z`（同B1）・`CHM202z`（同B2）の科目名が、`scripts/gen_data.py`の`INTRO`定義で全角の「Ａ１」「Ａ２」「Ｂ１」「Ｂ２」になっていた。実際のシラバスWeb公開システムの表記は半角「A1」「A2」「B1」「B2」で、`fetch_syllabus.py`の名前突き合わせ（`normalize_for_match`はローマ数字・ダッシュ・空白は正規化するが全角英数字は正規化しない）が一致せず、この4科目だけ**offeringsが一件も取得できていなかった**（他の全角英数字科目、例:哲学Ａ・生涯スポーツ演習Ａ等は、シラバス側も全角表記のため問題なく一致しており、対象はこの4科目のみと確認済み）
    - `scripts/gen_data.py`の`INTRO`を半角表記に修正し、`data/subjects/youran-2025.json`の該当4科目の`name`も直接修正。その上でこの4科目に絞ってシラバス一覧・個別ページを再取得し、各6〜7件（春/夏/秋/冬ターム制のクォーター単位の複数セクション）のofferingsを新規に取得できた
    - ただし**曜日時限は引き続き非表示のまま**：これらは1つの科目コードに複数のクォーター・複数セクションがあり、`class_assignment.json`にはこの4科目のデータが無い（`build_class_assignment.py`は英語系科目だけを除外しているので対象には含まれるが、今回のクォーター制のクラス分けは時間割PDFとの突き合わせがまだ行われていない）ため、`resolveSlotsForProfile`が一意に決められず`undefined`を返す。誤表示にはなっていないが、時限を表示するには、他の複数セクション科目と同様に`class_assignment_filled.csv`への追記（どのクォーター・どのセクションがどの1年次クラス向けかを時間割PDFで確認して記入）が別途必要
    - ついでに`fetch_syllabus.py`本体の別の潜在バグも発見・修正：個別シラバスページの取得URLが、一覧ページとは違い`SYLLABUS_YEAR`（2026）ではなく`"2025"`に直書きされたままだった（一覧のURLはSYLLABUS_YEAR経由で正しく2026年度分を見ていたが、科目コード・前提科目テキストの抽出元だけ2025年度分のページを見てしまっていた）。`SYLLABUS_YEAR`を使うよう修正
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過。Playwrightで名前が半角表示になること・consoleエラーが出ないことを確認済み

17. **基礎科学実験4科目分を`class_assignment_filled.csv`に追加（クラスは開発者が記入予定）し、ついでにCSV全体を2026年シラバスと突き合わせて監査（2026-09-06、開発者から「そもそもcsvファイルに項目自体が存在しないじゃん、追加しといて」「この際そのcsvファイルに不審な点がないか、2026のシラバスを見て念入りにチェックして」との指示）**：
    - `scripts/build_class_assignment.py`を実行し、16番で新たにofferingsが付いた`PHY101z`/`PHY201z`/`CHM101z`/`CHM202z`（各6〜7セクション、クォーター単位）ぶんの行（52行、`class_id`は空欄）を`class_assignment_filled.csv`に追加。一部は`class_schedule.csv`（A1.pdf）との自動突き合わせで`class_id`が既に埋まった（`status: auto`）
    - **監査の過程で、CSVに実際に記入済み（`status: filled`）だった6行が、現在のofferings（2026年度シラバス）のどのキーにも一致しない「孤立行」になっていることを発見**：`COM402m/n/p/r`・`COM403i/j`（計算機工学、Ⅲ類電子工学・光工学・物理工学・化学生命工学プログラムおよびⅡ類向け）が、CSV上は「木曜2限」のままだったが、実際のシラバス（`GakkiIchiran_31_0.html`を直接再取得して確認）では「金曜1限」（担当:遠藤　幸一）に変わっていた。おそらく2025→2026のシラバス年度切り替え（進捗ログ参照）の際、この科目の時限が実際に変更されたが、CSVには反映されていなかったもの。`class_id`（機械システムプログラム等の対象プログラム名／Mエリア）はそのまま引き継ぎ、`day`/`period`/`instructors`だけ実際の時限に修正し、`note`に修正した旨を追記した。なお`COM402k`（同じ「計算機工学」という科目名だが、機械システムプログラム向けの別講義。木曜2限に2セクション、担当は遠藤エミ／松本俊哉で、COM402m等とは別物）は、そもそもCSVに一件も無かった新規科目としてこのタイミングで追加した（`JPN101z`・`JPN201z`・`PHY102z`・`PHY202z`と合わせて計14行）
    - 監査の具体的な手順：①`data/subjects/youran-2025.json`の現在のofferingsから`(科目コード, 学期, 曜日, 時限)`キーの集合を作り、CSV全行のキーがそこに存在するか機械チェック（オンデマンド科目の空欄day/periodも`build_class_assignment.py`と同じ合成ロジックで再現し誤検出を除去）→孤立6行を発見。②`status: filled`の全1174行について、CSVに記録された担当教員名が現在のofferingsの同じキーの担当教員と1人でも重なるかを確認→上記6行以外に不一致なし。③`scripts/build_class_assignment.py`を再実行し、新規追加後の差分が意図通り0件（`removed`は上記6行のようにofferingsが1件しかない科目＝スクリプトの対象外になった科目のみ、これは正常）になることを確認
    - 作業中、複数の候補コードにまたがる行を一括で追記しようとして、既にキーが一致していた行まで重複コピーしてしまうミスが一度あったが、追記前のバックアップから復元し、「現在のCSVに無いキーだけ」を正確に絞り込んで追記し直した（最終的な重複行は無いことを確認済み）
    - `scripts/build_class_assignment_json.py`で`class_assignment.json`を再生成（1282件）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過。Playwrightで電子工学プログラムの「計算機工学」（COM402m、単一セクションになったため直接表示）が「金・1限」と正しく表示されること、機械システムプログラムの「計算機工学」（COM402k、まだ複数セクションでクラス未記入のため非表示のまま）が誤表示せず非表示のままであることを確認済み
    - **開発者への申し送り**：基礎科学実験4科目・`JPN101z`/`JPN201z`/`PHY102z`/`PHY202z`/`COM402k`の新規追加分（`status`が空欄の行）は、時間割PDFを見て`class_id`を記入すれば曜日時限が表示されるようになる

18. **細かな表示バグ6件を修正（2026-09-06、開発者からのバグ一覧報告）**：
    - **政治学Ａ（HSS312z）が「オンデマンド」と誤表示される不具合**：実際のシラバスを確認したところ開講学期欄が「前学期集中」（対面授業）で、`note`フィールド自体が未設定だった（`生涯スポーツ演習Ｃ/Ｄ`は既に`note: "夏期集中講義"/"冬期集中講義"`が設定済みで、オンデマンド扱いにならず「何も表示しない」になっていた）。`scripts/gen_data.py`のHSS定義・`data/subjects/youran-2025.json`に`note: "夏期集中講義"`を追加
    - **「夏期集中」「冬期集中」科目の表示ロジックを追加**：`src/pages/MainPage.tsx`の`dayPeriodTag`が、これまでnoteに「集中」を含む科目を一律「何も表示しない」にしていたのを、`note`が「夏期集中」「冬期集中」を含む場合はその文言自体を表示するように変更（`政治学Ａ`→「夏期集中」、`生涯スポーツ演習Ｃ`→「夏期集中」、`Ｄ`→「冬期集中」。それ以外の「隔年度開講」等の集中講義は従来通り非表示のまま）
    - **人文・社会科学科目の前学期/後学期の折りたたみに「夏期集中」の入れ子を追加**（開発者提案）：政治学Ａのように`termType`は「前学期」のままだが実際は夏期の集中講義、という科目を前学期・後学期の折りたたみとは別の「夏期集中」（該当科目があるときだけ表示）にまとめるようにした。`intensiveSeasonOf()`という新しいヘルパーを追加し、`GroupProgress`コンポーネントにpropとして配線（同コンポーネントは`MainPageContent`とは別のトップレベル関数なのでクロージャで共有できず、明示的にpropで渡す必要があった）。生涯スポーツ演習（`health`グループ）は元々前学期・後学期での折りたたみ自体をしていないグループなので、こちらは何も変更していない（開発者の指示通り）
    - **クォーター（ターム）制で単一タームしか無い科目は「N年◯ターム」と表示するように変更**：`アカデミックスキルズ`（CAR201z）はこれまで単一offering・単一slot（木・5限）としてそのまま「木・5限」と表示されていたが、実際はシラバス上「夏ﾀｰﾑ」開講の科目なので、`dayPeriodTag`に新しい分岐を追加し「1年夏ターム」と表示するようにした。`QUARTER_LABELS`という新しい定数（半角カナ表記→表示用の全角表記の対応表）を追加。基礎科学実験のように1つの科目コードで複数のタームにまたがる科目は、この新しい分岐の対象外（従来通り複数セクション科目としてクラス割り当て解決に回る）ことを確認済み
    - **学域特別講義A/B（UEC001z/UEC002z）に曜日時限を追加**：この2科目は年度ごとに具体的なテーマ・担当教員が変わる特別講義で、シラバス一覧上の科目名にはその年のテーマが入るため名前の完全一致では拾えない（`note: "開講年度により扱いが異なる"`として元々別枠扱いにしていた）。ただし開講される曜日時限自体は毎年固定（前学期木5＝A、前学期金5＝B）と開発者から確認が取れたため、`scripts/fetch_syllabus.py`に固定値を補うオーバーライド処理を追加（`CODE_SWAP_FIXUPS`と同様、`offerings_by_code`への`setdefault`。将来もし名前一致で本当に取得できるようになった場合はそちらを優先する設計）。`data/subjects/youran-2025.json`にも同じ内容を直接反映
    - **注記（note）を実際の時限表示に併記する仕組みを追加**：上記の学域特別講義のように「毎年内容は変わるが曜日時限は固定」という事情が伝わるよう、`dayPeriodTag`が実際に曜日時限・ターム名を表示するときは、その科目の`note`があれば「（開講年度により扱いが異なる）」のように併記するようにした。既存の科目で`note`と実際のslotsを両方持つものは15件確認したが、いずれも「隔年度開講」等の有用な情報であり、表示が増えて困る科目は無いことを確認済み
    - **上級科目（`advanced`）は今回のバグ一覧確認で問題なしと開発者から報告済み**（TestCase1データにおいて）。他のグループ・科目についてはまだ未確認とのことなので、今後追加の報告があれば都度対応する
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過。Playwrightで6件すべての表示が意図通りになったこと（政治学Ａ「夏期集中」・生涯スポーツ演習Ｃ「夏期集中」・Ｄ「冬期集中」・アカデミックスキルズ「1年夏ターム」・学域特別講義A「木・5限（開講年度により扱いが異なる）」・B「金・5限（同）」）、および人文・社会科学科目の折りたたみに「夏期集中（1）」の入れ子が新規に現れ政治学Ａだけが入っていることを確認済み

19. **必修の1・2年次英語（ENG101z/102z/201z/202z/301z/401z）をclass_assignmentの対象に追加し、記入待ちのワークシートCSVを開発者へ送付（2026-09-06、開発者から「基礎科学実験A1/A2/B1/B2、必修の英語系は今のデータじゃ無理でクラスとか入力しないといけなさそう」との指示）**：
    - 当初「英語系はクラス分けを聞かずに時限レンジ表示で済ませる」方針だったが未実装のままだったため、開発者の方針転換で必修の1・2年次Academic English（ENG101z・ENG102z・ENG201z・ENG202z・ENG301z・ENG401z。TEN501z等の他の英語系・上位学年の選択科目は対象外のまま）だけ、クラス別の曜日時限解決の対象にすることにした。`scripts/build_class_assignment.py`の「名前に"English"を含む科目は対象外」という除外条件に、この6科目コードだけの例外リスト（`ENGLISH_CLASS_ASSIGNMENT_CODES`）を追加
    - 再実行して158行（ENG101z26・ENG102z25・ENG201z26・ENG202z25・ENG301z28・ENG401z28）を`class_assignment_filled.csv`に追加（`class_id`は空欄）。既存の基礎科学実験4科目分と合わせて、監査済みの手順（キー整合性チェック・完全一致重複チェック）で問題ないことを確認
    - `scripts/build_class_assignment_json.py`で`class_assignment.json`を再生成（既存の1282件のまま。空欄の`class_id`行はJSONには出力されないため件数は変わらない、正常な挙動）
    - **開発者からの依頼「必要そうな科目をまとめたファイルを出して、classidと学期を入力するだけで完了するような」に対応**：`class_assignment_filled.csv`から、対象10科目（基礎科学実験4科目＋今回追加した英語6科目）のうち`class_id`がまだ空欄の行だけを抜き出したワークシートCSV（194行。一部は`class_schedule.csv`との自動突き合わせで既に埋まっていたので、それらは含まれていない）を作り、`SendUserFile`で開発者に送付した（リポジトリには含めていない一時ファイル）。列は`class_assignment_filled.csv`と同じ（`subject_code,subject_name,term,day,period,instructors,class_id,status,note`）で、`term`列は既存データをそのまま確認用に残しているだけで入力不要、`class_id`列だけ埋めれば良い設計
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過
    - **開発者への申し送り**：送付したワークシートCSVに`class_id`を記入して返してもらえれば、`class_assignment_filled.csv`の該当行にマージし、`class_assignment.json`を再生成する（次回の作業）

20. **開発者から報告された細かなバグ一覧の調査・修正（2026-09-06）**：
    - **時限が表示されない9科目を調査**：数学演習第一・第二、物理学概論第一、線形代数学第二、微分積分学第二、基礎プログラミングおよび演習、離散数学、情報領域演習第一、複素関数論。うち8科目（複素関数論以外）は**データもコードも正常で、プロフィールのクラス項目（`classIABC`等）が「未定」のままだと仕様通り非表示になっているだけ**と判明（Playwrightで`classIABC=A`を指定したところ全て正しく表示された）。**複素関数論（Ⅰ類側のMTH304a/c/d/e：media・mathinfo・cs・designds）だけ実際のバグ**で、Ⅲ類の`MTH302i`等と同一のtimetableCode（＝合同講義）を共有しているのに、Ⅰ類側の`class_id`が誤ってⅢ類専用の表記「Mエリア(Nクラス)」のままコピーされており、Ⅰ類のプロフィールでは絶対に一致しない状態だった。一度「全クラス」に修正したが、**開発者から「複素関数論は全クラスの授業じゃない」と指摘があり修正を取り消し**。受講対象の決め方（プログラムごとか学籍番号ごとか等）を尋ねたところ「自分で編集する」とのことで、`class_assignment_filled.csv`の該当行は開発者が直接編集する方針になった（Claudeはこれ以上手を入れない）
    - **「イノベイティブ総合コミュニケーションデザイン2」（CAR601z）に曜日時限が無い不具合**：シラバス側の科目名が全角数字「２」（イノベイティブ総合コミュニケーションデザイン**２**）で、科目マスタの半角「2」と一致していなかったのが原因（基礎科学実験のA1/B1等と同じ「全角・半角の表記ゆれ」パターン）。今回は個別に直すのではなく、`fetch_syllabus.py`の`normalize_for_match()`に**全角英数字・全角記号を半角に変換する一般ルール**（`FULLWIDTH_TO_HALFWIDTH`、Ｕ+FF01〜FF5EをＵ+0021〜007Eへ一括変換）を追加し、今後同種の科目でも自動的にマッチするようにした（哲学Ａ・生涯スポーツ演習Ａ等、シラバス側も全角のまま一致している科目は変換後も両辺が揃うので影響なし）。`data/subjects/youran-2025.json`のCAR601zにも直接offeringsを反映（木・5限、後学期、山田祥之）
    - **「知的財産権」「技術者倫理」の昼間版（CAR603z/CAR604z）が夜間主のデータを表示していた不具合**：開発者から「昼間は技術者倫理→水1、知的財産権→月1のはず」と指摘され、実際のシラバス個別ページの科目番号欄を直接取得して確認したところ、**大学のシラバスシステム側の登録ミス**と判明：夜間主の個別ページ（知的財産権=22018104・技術者倫理=22018205）の科目番号欄に、夜間コード（CAR701s・CAR801s）と昼間コード（CAR603z・CAR604z）が**両方まとめて**書かれている一方、昼間の個別ページ（21018233・21018234）の科目番号欄は**空欄**になっていたため、CAR603z/CAR604zは夜間の時限（水7）しか取得できていなかった（ENG101s/zの入れ替わりバグと同系統だが、今回は「入れ替わり」ではなく「昼間側の欄が空欄」という別パターン）。`fetch_syllabus.py`に、一覧ページの行データから直接値を補うオーバーライドを追加（`CAR603z`→月1・本間ほか、`CAR604z`→水1・濁川義和。CAR701s/CAR801sは夜間の時限のままで正しいので変更なし）。`data/subjects/youran-2025.json`にも直接反映
    - **`class_assignment_filled.csv`が編集中でロックされ、書き込みに失敗する事態が発生**：`PermissionError`が続き調査したところ、**前日（2026-09-05）から起動しっぱなしだった別セッションのvite開発サーバー（ポート5199）がファイルを掴んでいた**ことが原因と判明（プロセスを終了して解消）。ロック解除後にファイルを読み直すと、**開発者が同時にこのファイルを直接編集していた**内容（行の並び替え、`ENG` 系科目への`class_id`記入など）が反映された状態になっていた。Claude側の変更はその上に重ねる形で保存し、開発者の編集内容を壊していないことを確認した。**教訓**：devサーバーは使い終わったら確実に停止すること（`netstat`のPIDでの停止だけでなく、`Get-CimInstance Win32_Process`でコマンドラインを見て取りこぼしが無いか確認するとよい）
    - **`class_id`列に複数時限をまとめて書けるように対応**：開発者が`CHM101z`（基礎科学実験B1）の月曜3・4限の行を、担当教員・`class_id`が同じだからと`period`列に「3，4」とまとめて記入していたのを発見。「3限・4限で1つの科目」を1行にまとめられた方が記入の手間が減るという開発者の要望を受け、`scripts/build_class_assignment_json.py`が`period`列を`,`/`，`で分割し、`period`ごとに別々のJSONエントリ（同じ`classIds`）に展開するよう対応（`data/timetable/README.md`にも記法を追記）。既存の1period=1行のままの行にも影響が無いことを確認済み
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（89件）・`npm run lint`・`npm run build`すべて通過。Playwrightで「イノベイティブ総合コミュニケーションデザイン2」→木・5限、「知的財産権」→月・1限、「技術者倫理」→水・1限と正しく表示されることを確認済み
    - **開発者への申し送り**：`ENG401z`等に記入された「1限→一類、2限→2類、3限→3類」という新しい`class_id`記法（時限によって受講対象の類が決まる）は、現状の`classIdMatchesProfile`ではまだ解釈できない。対応するには表記のルールを確認した上で`src/domain/classAssignment.ts`にパーサーを追加する必要がある（今回は着手していない）→ **21番で対応済み**

21. **`resolveSlotsForProfile`のクラス別セクション重複バグを修正し、「一類/二類/三類」表記に対応（2026-09-06、開発者が「Bクラスと設定していても数学演習等が表示されない」「（ENG301z等の記法は）1限のその科目は1類全員が受ける、という意味でnoteに書きたかった」と報告）**：
    - **根本原因**：`MTH205a`（離散数学）の月曜1限のように、「同じ(科目・学期・曜日・時限)にクラスごとに担当教員が違う複数のセクションがある」科目では、`scripts/build_class_assignment_json.py`がCSVの行ごとに別々のJSONエントリを作るため、`class_assignment.json`に同じキーのエントリが複数（Aクラス用・Bクラス用など）並ぶ。`src/domain/classAssignment.ts`の`resolveSlotsForProfile`内`offeringMatches`が`assignments.find(...)`で**最初の1件しか見ていなかった**ため、配列の2番目以降（Bクラス等）に対応する学生は永遠に一致しなかった。`.find`を`.filter`に変え、該当する全エントリのclassIdsを候補にするよう修正。単体テスト1件追加（`MTH205a`を模した実データ相当のケース）
    - **「一類/二類/三類」表記に対応**：`ENG301z`/`ENG401z`（Academic English for the 2nd Year）で、開発者が「1限→一類、2限→2類、3限→3類」（時限によって受講する類が決まる、という意味の説明）を誤って`class_id`列に直接書いてしまっていた。意図を確認したところ「本当はnoteに書きたかった」とのことだったので、`classIdMatchesProfile`（`src/domain/classAssignment.ts`）に**プログラムや1年次クラスに関係なくその類全員が対象**という新しい表記「一類」「二類」「三類」を追加し、該当56行の`class_id`を実際の値（1限の行→`一類`、2限→`二類`、3限→`三類`）に修正、説明文は`note`列に移した
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（90件）・`npm run lint`・`npm run build`すべて通過。Playwrightで`classIABC=B`を指定した数学演習第一等が正しく表示されること、Ⅰ類のAcademic English for the 2nd Yearが「火・1限」、Ⅲ類（機械システム）が「火・3限」と正しく類ごとに出し分けられることを確認済み

22. **複素関数論・Technical Englishの行をclass_assignment_filled.csvから切り出し、開発者に送付（2026-09-06、開発者から「複素関数論だけ後でマージする用のファイルに切り出して。手で修正する」「Technical Englishも手で修正するから複素関数論同様にファイルに切り出して」との指示）**：
    - **複素関数論**：`class_assignment_filled.csv`から`subject_name`が「複素関数論」の全30行（Ⅰ類のmedia/mathinfo/cs/designds＝MTH304a/c/d/e、management＝MTH304b、Ⅱ・Ⅲ類の各コード＝MTH302系・MTH304f〜h）をそのままの値で抜き出し、`class_assignment_filled.csv`本体からは削除した。21番より前の時点でⅠ類側のMTH304a等に見つかっていた「Mエリア」の誤りも含め、開発者が直接編集する
    - **Technical English**：`scripts/build_class_assignment.py`の`ENGLISH_CLASS_ASSIGNMENT_CODES`に`TEN501z`（Technical English – Basic English for Science）・`TEN601z`（同 – Intermediate English for Science。開発者指摘の通り「Technical English」だけでは正式名称ではなく、末尾に副題が付く2科目）を追加し、英語系の除外対象から外した。再実行して新規に60行（各30セクション、class_idは空欄）を生成し、`class_assignment_filled.csv`には追加せず別ファイルとして抜き出した
    - 両ファイルとも`SendUserFile`で開発者に送付（`fukuso_kansuuron_extract.csv`30行・`technical_english_extract.csv`60行、いずれもリポジトリには含めない）。`class_assignment_filled.csv`からこの2科目分（複素関数論30行）が抜けたことで、`class_assignment.json`の件数は1447→1417に減少（＝この2科目は開発者が直すまで一時的に曜日時限が非表示になる。マージし忘れないよう申し送り）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（90件）・`npm run lint`・`npm run build`すべて通過
    - **開発者への申し送り**：修正済みファイルを受け取ったら、`class_assignment_filled.csv`に該当行をマージし、`scripts/build_class_assignment_json.py`で`class_assignment.json`を再生成する（次回の作業）

23. **基礎科学実験A1/B1・Academic English（1年）の時限が表示されない不具合を修正（2026-09-06、開発者が「基礎科学実験系の時限が表示されない」「英語(1年)のやつが全部時限表示されてない」と報告）**：
    - **基礎科学実験A1/B1（`PHY101z`/`CHM101z`）**：`class_id`に入っていた「A1-7」のような値（`scripts/build_class_assignment.py`が`class_schedule.csv`との自動突き合わせで生成する「{pdf名}-{class_id}」形式）を、`classIdMatchesProfile`（`src/domain/classAssignment.ts`）がこれまで一切解釈できていなかったことが根本原因。A1・A2（1年前期・後期）はどちらも1年次クラス1〜12と同じ番号のため、「クラスN」と同じ扱いになるよう`/^A[12]-(\d+)$/`のパターンを追加。単体テスト1件追加。他に同じ形式を使っている科目は`PHY101z`/`CHM101z`の2つだけと確認済み（`PHY201z`/`CHM202z`はそもそも`class_id`が全部空欄で該当なし）
    - **Academic Written/Spoken English（1年）（`ENG101z`/`102z`/`201z`/`202z`）**：`class_id`列に、また前回（19番参照）と同じパターンで説明文（例:「1限→1，2，3クラス、2限→4，9，10クラス…」）がそのまま入ってしまっていた。テキストをパースして、時限ごとの正しい`class_id`（例:1限の行なら`クラス1,クラス2,クラス3`）に変換し、説明は`note`列に移した（102行）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（91件）・`npm run lint`・`npm run build`すべて通過。PlaywrightでⅡ類情報通信工学プログラム・1年次クラス7で、基礎科学実験A1が「月・3限/月・4限」、Academic Written/Spoken English Ⅰ・Ⅱが正しい時限（クラス7が該当する時限）で表示されることを確認済み。基礎科学実験B1/B2の残りの空欄（`class_id`未記入分）は、送付済みの`class_id_worksheet.csv`が埋まっていないだけの想定通りの状態
    - **チャット添付ファイルの受け渡し方法を変更**：開発者から「添付ファイルの開き方がわからない」と報告があり、この環境がユーザー自身のPC上でローカルに動いていることを踏まえ、以後はチャット添付（`SendUserFile`）ではなく`C:\Users\maita\Downloads\`直下にファイルを直接保存する方式に変更した（エクスプローラーでそのまま開ける）

24. **複素関数論（`fukuso_kansuuron_extract.csv`）の修正分をマージ（2026-09-06、開発者が編集して「複素関数のやつ書いたから反映させて」と依頼）**：
    - 開発者が`Downloads`フォルダの切り出しファイルを編集して返してきた。management（`MTH304b`）の`class_id`が、以前の「Aクラス,Bクラス」「Bクラス,Cクラス」（2年前期A/B/Cクラス方式）から「1クラス、2クラス」「3クラス、4クラス」（数字が先の全角読点区切り）に変更されていた。数字が先の表記は過去（中国語第一/第二等）にも見つかった書き方で、`classIdMatchesProfile`の「クラスN」パターン（`/^クラス(\d+)$/`、Nが後ろ）とは順序が逆で一致しないため、マージ時に「クラス1,クラス2」の順に正規化した（2行）
    - **Ⅰ類のmedia・mathinfo・cs・designds（`MTH304a`/`c`/`d`/`e`）は今回の編集で触れられておらず、以前から問題だった「Mエリア(Nクラス)」（Ⅲ類専用表記、Ⅰ類では絶対に一致しない）のまま**。開発者への確認は既に済んでいて「自分で編集する」との回答だったため、Claude側からは引き続き手を入れていない
    - `class_assignment_filled.csv`にマージ（1489行）→`class_assignment.json`再生成（1447件）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（91件）・`npm run lint`・`npm run build`すべて通過。Playwrightで確認：Ⅰ類management・1年次クラス1で複素関数論が「火・4限」と正しく表示、Ⅰ類media・1年次クラス1では（想定通り）まだ非表示のまま
    - **開発者への申し送り**：media/mathinfo/cs/designds分の`class_id`修正はまだ未反映。修正が終わったら同様に反映する

25. **複素関数論（Ⅲ類側）の「Ⅲ-N」表記を新設し、Technical Englishのデータ不整合を修正（2026-09-06）**：
    - **複素関数論**：開発者から「そもそもクラス間違えてた」と指摘。Ⅲ類側（`MTH302i/j/k/m/n/p/r`・`MTH304a/c/d/e/f/g/h`）の`class_id`「Mエリア(1クラス)」等は、実際は「Ⅲ類の2年前期クラスN（エリアに関係ない）」という意味だった。「クラスN」（1年次クラス）を類によって意味が変わる形にするのは紛らわしいという開発者の提案（「Ⅲ-1とかで管理できないの？」）を受け、新表記`Ⅲ-N`を追加（`classIIIYear2Class`と照合、Mエリア/Sエリアの区別はしない）。単体テスト1件追加。management（`MTH304b`）は元々別講義（別timetableCode）で「クラスN」（1年次クラス）のままが正しく、対象外
    - **`resolveSlotsForProfile`を拡張**：曜日時限が食い違う複数のセクションが一致した場合、一致した`classId`が全部「同じプログラム名」なら、決め打ちせず該当する時限を全部列挙するように変更（例:Technical Englishの「デザイン思考・データサイエンスプログラム」がI19クラス/I20クラスで2つの時限に分かれているケース）。「クラスN」等の個別番号が食い違う場合は、データの矛盾の可能性が高いため従来通り非表示のまま（テストで区別を確認）
    - **Technical English（`TEN501z`/`TEN601z`）を新規に対象化・データ投入**：開発者が切り出しファイルに記入して返してきたが、「レイヤーを引っ張る」（Excelでのセル範囲コピー）操作でコード・曜日・時限がずれた行が複数混入していた。実際の科目マスタ（`offerings`）と突き合わせて機械的に検証し、存在しない曜日時限を参照している行（1件、`Roderick Lange`の「金1限」→実際は「金2限」で別プログラム名として正しく登録済み）を検出・削除。`class_id`が「、」（読点）区切りで複数プログラム名を書く記法だったため、`scripts/build_class_assignment_json.py`のclass_id分割を`,`・`，`・`、`のどれでも対応するよう修正。「上級者」「再履生」向けの特別セクションはプロフィールに対応する情報が無い/既存の`再履生`表記があるため、意図通り一般学生には表示されない
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（93件）・`npm run lint`・`npm run build`すべて通過。Playwrightで確認：designds（デザイン思考・データサイエンスプログラム）のTechnical Englishが「木・1限/木・3限」「金・2限/金・3限」と2枠とも正しく列挙されること、mecha（機械システムプログラム）は誤った金1限データを削除後「木・1限」の単一表示に修正されたことを確認済み
    - **開発者への申し送り**：基礎科学実験系（`PHY101z`/`PHY201z`/`CHM101z`/`CHM202z`、51行）を`Downloads\kiso_kagaku_jikken_extract.csv`に切り出し済み（`class_assignment_filled.csv`からは削除済みで一時的に非表示）。記入が終わったら教えてもらえればマージする

26. **Technical Englishの教員行の抜けを2件補完・1件は保留（2026-09-06、開発者が「存在してるのに登録してないのが目立った。Kobayashi先生のやつとか」と指摘）**：
    - Kobayashi先生（Kimi Estela Kobayashi-Cuya）分は開発者が既に追記済みと確認。他に、同じ時限を担当する教員のうち行が無い3件（`TEN601z`後学期・木曜1/2/3限のRoderick Lange・大石由紀子・Dermot Lonergan）を機械的に検出
    - 木2限（大石由紀子）・木3限（Dermot Lonergan）は、その時限の他の教員行が全部同じ`class_id`だったので、同じ値で安全に補完した
    - 木1限（Roderick Lange）は、その時限に「セキュリティ情報学プログラム」と「機械システムプログラム」の2つの`class_id`が混在しており、どちらに該当するか判断できないため未補完のままにしていたが、開発者が「機械システムプログラム」と判断して記入・反映済み（2026-09-06）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（93件）・`npm run lint`・`npm run build`すべて通過
    - **開発者からの依頼で追加：シラバス・データ全体の見直しをタスクに追加**（次回以降にやること、下記参照）

27. **残タスク（2026-09-06追加、未着手）**：`data/subjects/youran-2025.json`の`offerings`・`data/timetable/class_assignment_filled.csv`のデータ全体を、シラバス（`https://kyoumu.office.uec.ac.jp/syllabus/2026/`）と改めて突き合わせて見直す。Technical Englishで「時限自体はカバーされているが個別の教員の行が抜けている」（26番）・「レイヤーを引っ張る操作でコード/曜日/時限がずれる」（25番）といった見落とし・入力ミスが見つかったため、他の科目でも同様の抜け・ズレが無いか広く点検する必要がある、という開発者の指摘。具体的な対象範囲・優先順位はまだ相談していない

28. **基礎科学実験系（`PHY101z`/`PHY201z`/`CHM101z`/`CHM202z`）を`kiso_kagaku_jikken_extract_dedup.csv`からマージ（2026-09-06）**：
    - 開発者が、同じ教員・同じ`class_id`になる複数の行（クォーター違い・period3/4の別行）を1行にまとめ直した「dedup」版（26行、元の51行から削減）を作成。`period`列の「3，4」まとめ書きに加え、`class_id`列にも「クラス7，8」（「クラス」1回＋数字を区切り文字でまとめる省略記法）という新しい書き方が使われていた
    - このままだと「クラス7，8」を単純にカンマ分割すると「クラス7」と（「クラス」が付かない）「8」という意味不明なトークンに壊れてしまうため、`scripts/build_class_assignment_json.py`に`expand_class_shorthand()`を追加し、分割前に「クラス7，8」→「クラス7,クラス8」に展開するようにした
    - `class_assignment_filled.csv`にマージ（26行、対象4科目はこれまで一件も本体に無かったことを確認した上で追加）。`class_assignment.json`再生成
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（93件）・`npm run lint`・`npm run build`すべて通過。Playwrightで、Ⅱ類情報通信工学プログラム・1年次クラス7で基礎科学実験A1/A2/B1/B2の4科目とも「月・3限/月・4限」と正しく表示されることを確認済み
    - Downloadsの`kiso_kagaku_jikken_extract.csv`（マージ元と同名のコピー）も最新状態に更新した

29. **複素関数論：Ⅰ・Ⅱ類側の科目コードは古川先生/遠藤先生の講義（Ⅲ類向け）に紐付いていたこと自体が誤りと判明、該当行を削除（2026-09-06）**：
    - 前回、`MTH302i`・`MTH302j`・`MTH304f`・`MTH304g`・`MTH304h`をⅢ類のプログラムだと誤認していたことが判明（実際はⅡ類：計測・制御システム・先端ロボティクス・セキュリティ情報学・情報通信工学・電子情報学）。プログラム名を併記した切り出しファイル（`fukuso_kansuuron_extract2.csv`）を渡して確認してもらったところ、**Ⅰ類（`MTH304a/c/d/e`）・Ⅱ類（`MTH304f/g/h`・`MTH302i/j`）の学生は、そもそも古川怜先生・遠藤晋平先生の講義を受けない**（実際は宮脇先生・小木曽先生等の別講義を受ける）ことが判明。これら9科目コードがこの合同講義（Ⅲ類向け）に紐付いていたこと自体が科目コードの誤りだった
    - 該当9科目コード分の`class_assignment_filled.csv`の行（18行、古川先生・遠藤先生分）を削除。本来のⅢ類5科目（`MTH302k/m/n/p/r`）とmanagement（`MTH304b`、別講義）は変更なし
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（93件）・`npm run lint`・`npm run build`すべて通過
    - **開発者への申し送り**：`data/subjects/youran-2025.json`側の`offerings`（この9科目コードの科目マスタデータ）自体も、まだ古川先生/遠藤先生の講義（水3限）のtimetableCodeを指したままになっている。宮脇先生・小木曽先生の本来の講義（曜日時限・timetableCode）をシラバスから別途調べて`offerings`を差し替える必要があるが、今回はclass_assignment側の削除のみで、科目マスタ側の修正はまだ着手していない → **30番で対応完了**

30. **複素関数論：Ⅰ類・Ⅱ類側の正しい講義データを2026年シラバスから取得して修正完了（2026-09-06）**：
    - 開発者からリンク（`31_21122118.html`等7件）をもらい、各ページを直接取得して精査。**大学のシラバスシステム自体に登録ミスがある**ことを確認：「複素関数論（Mエリア）」（宮脇陽一先生、水3限）・「複素関数論（Ｉエリア）」（鈴木淳先生／小木曽公尚先生、木3限）の科目番号欄は完全に空欄（`<td colspan="3"></td>`）で、Ⅱ類の5科目コードはどこにも登録されていなかった。一方「複素関数論（Ⅲ類）」（古川怜先生／遠藤晋平先生、水3限）の科目番号欄には、本来登録されるべきでないⅠ類（`management`以外の4つ）・Ⅱ類（5つ）の科目コードまで全部まとめて誤登録されていた
    - 開発者確認：**Ⅰ類は南泰浩先生（火4限）・龍野智哉先生（水4限）、Ⅱ類は宮脇陽一先生（Mエリア、水3限）・鈴木淳先生／小木曽公尚先生（Ｉエリア、木3限）を受講する**（Ⅲ類は古川先生・遠藤先生のまま変更なし）。また「複素関数論を受講する2年前期の時点ではまだプログラムが確定していないので、科目コードの末尾記号（a/c/d/e等）自体に意味は無い」との指摘があり、Ⅰ類側4科目・Ⅱ類側5科目はそれぞれ同じ類の中では全プログラム共通のofferings・class_idにした
    - `data/subjects/youran-2025.json`の該当9科目コードの`offerings`を、南先生/龍野先生（Ⅰ類4科目）・宮脇先生/鈴木先生/小木曽先生（Ⅱ類5科目）の実際のtimetableCode・曜日時限に差し替え。`class_assignment_filled.csv`にも該当23行（Ⅰ類4科目×2行＋Ⅱ類5科目×3行）を追加（`class_id`はⅠ類が`クラス1,クラス2`/`クラス3,クラス4`＝MTH304bと同じ1年次クラス分け、Ⅱ類が`Mエリア`/`Iエリア`）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（93件）・`npm run lint`・`npm run build`すべて通過。Playwrightで5パターン（Ⅰ類media・designds、Ⅱ類netinfo・security、Ⅲ類mecha）すべて正しい曜日時限（火4/水4/水3/木3/水3）が表示されることを確認済み。これで複素関数論は15科目コード全部が解決した

31. **「その他単位認定」機能を追加（2026-09-06、開発者から「共通単位にその他単位認定を追加して、未履修または1〜8単位(9択)を選べるようにして欲しい。共通単位って科目以外でも取れるから」との依頼）**：
    - TOEIC等、特定の科目を介さずに認定される共通単位を入力できるようにした。`src/domain/requirements.ts`の`evaluateRequirements()`に第4引数`otherCommonCredits`（省略時0、既存の呼び出し元に影響なし）を追加し、共通単位の集計にそのまま加算するようにした（`commonCredits`の上限で頭打ちになる既存のロジックはそのまま適用される）
    - `src/storage/otherCommonCredits.ts`を新設（`records.ts`と同様のlocalStorage読み書き）。`MainPage.tsx`に他の科目記録と同じdraft/committed方式（「更新」ボタンを押すまで反映しない）で状態を追加し、「選択科目」セクションの「共通単位」折りたたみの先頭に「未履修/1〜8単位」の9択セレクトを追加。「リセット」ボタンを押したときもこの値を0に戻すようにした
    - 単体テスト3件追加（そのまま加算される・他の共通単位源と合算されてcommonCreditsの上限で頭打ちになる・省略時は0）。`npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`すべて通過。Playwrightで実際に5単位を選んで更新→見出しが「5/8単位」に変わること、リロード後も値が保持されることを確認済み
    - **スコープ外**：本サイト形式JSON（ダウンロード/読み込み）のexport/importスキーマにはまだ組み込んでいない（`localStorage`のみ）。必要になれば`schemaVersion`を上げて対応する

32. **残タスク（2026-09-06追加、未着手）**：開発者から「あとやりたいこと」として以下の要望があった。優先順位・詳細仕様はまだ相談していない
    - 「その他単位認定」を本サイト形式JSON（ダウンロード/読み込み）のexport/importにも対応させる（31番のスコープ外事項）
    - サイト名を「電通大 単位取得ルートナビ(β版)」へ変更
    - トップ画面のサイト名の右横に最終更新日を追加
    - 科目名クリックでシラバスへリンクが飛ぶようにする（`data/subjects/youran-2025.json`の`offerings[].syllabusUrl`を使う想定。offeringsが無い科目・複数セクションで代表URLが決められない科目をどう扱うか要検討）
    - 転類・転プログラムの実装（他の類・他のプログラムへ移った学生への対応。留学生・再履生と同様、プロフィールに新しい項目が必要になる可能性がある設計判断待ちの課題。CLAUDE.md本文8番の「留学生」「転類生」の申し送りと関連）
    - **「プログラム比較」（`/compare`、`src/pages/ComparePage.tsx`）と「データ」（`/data`、`src/pages/DataPage.tsx`）ページは削除してよいかも、との開発者の意見**：`ComparePage`はF-6として元々スコープ外・未実装のプレースホルダーのまま。`DataPage`は「全消去」ボタンのみ残っているが、MainPage.tsxの「リセット」ボタン（其の他単位認定含めて全部リセットするよう31番で拡張済み）と機能が重複しており、実質使われていない。削除する場合はナビ（`App.tsx`のリンク・ルート）も合わせて外す。まだ「消していいかも」という留保付きの発言なので、実行前に確認する
    - **科目一覧（F-5、これまでスコープ外だった機能）を実装する際の方針**：学修要覧の構成をそのまま踏襲する形でよい。同じ科目名が複数プログラムの類専門科目として重複登場すること（例:「コミュニケーション論」等）自体は問題なく、プログラムごとに重複して出しても構わないので、見出し・分類分け（プログラム名や区分ごとの階層）さえ分かりやすくなっていればよい、とのこと

33. **入学年度の入力欄を一時的に非表示化（2026-09-06、開発者判断）**：`entryYear`の実際の使われ方（`getRequirementSet()`の検索キー・ファイル名等）を確認したところ、`data/requirements/`以下が現状すべて`entryYear: 2025`のデータしか無く、選択肢を出しても実質意味が無いことが分かった。開発者は2024・2026年度分のデータも手元にはあるが、同じ量のデータ化作業をもう一度やる気力が今は無いとのことで、`SHOW_ENTRY_YEAR_INPUT`という真偽値定数（`src/pages/SetupPage.tsx`冒頭）を`false`にして入力欄を非表示にした（内部的には2025固定のまま動作。`entryYear`の状態・ロジック自体は削除せずそのまま残してあるので、**この定数を`true`に戻すだけで復活する**という、開発者の「あとですぐ戻せるように」という要望に沿った実装）。`npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`すべて通過。Playwrightで入力欄が消えていること・プロフィールは引き続き「2025入学」と正しく表示されることを確認済み
    - **申し送り**：2024・2026年度データを追加する気になったら`SHOW_ENTRY_YEAR_INPUT = true`に戻す。ただし着手前に「カリキュラムが本当に年度で違うのか」を確認し、差が無ければ`entryYear`を表示用ラベル程度に留めてデータ自体は使い回す、という選択肢も検討する（32番の作業量への懸念を踏まえた提案）

34. **「このサイトについて」ページ（`/about`）を実装（2026-09-06、開発者から作成者情報・問い合わせ先・作成目的の文面を渡されて依頼、`main`ブランチで直接作業してマージしてよいとのこと）**：
    - これまで`準備中`のプレースホルダーだった`src/pages/AboutPage.tsx`に、作成者（あず・電気通信大学2年）・不具合や要望の連絡先（Twitter・マシュマロへの外部リンク）・サイトを作った目的（学修要覧が分厚い／何を取れば卒業できるか分からない／審査が不安、という3つの動機）を実装した。外部リンクは`target="_blank" rel="noopener noreferrer"`を付けて開くようにした
    - `npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`すべて通過。Playwrightで表示内容・リンク先（href/target/rel）を確認済み
    - `dev`ブランチで作業し、`main`にマージ済み（開発者からマージの許可あり）

9. **「全クラス」が他のクラス指定と混在する科目で曜日時限が出ない不具合を修正（2026-09-06、開発者が「生涯スポーツ演習Ａの時限が出ていない」と報告して発覚）**：`HSP301z`/`HSP401z`（生涯スポーツ演習Ａ/Ｂ）を調べたところ、「全クラス」は文字通り「誰でも」ではなく、**「3年次までに未修得の学生向けの補講枠」**という意味で、通常のクラス/エリア/プログラム別セクションと**混在**していた（他の「全クラス」しか無い語学演習等とは違うパターン）。これまでの実装は「全クラス」を無条件一致にしていたため、本来のクラス別セクションと重複してしまい、曜日時限が一意に決まらず非表示になっていた。`resolveSlotsForProfile`（`src/domain/classAssignment.ts`）を、まず「全クラス」を除いた表記だけで一致を探し、1件でも見つかればそちらを優先する（見つからない場合だけ「全クラス」も候補にする）という2段階の判定に修正。単体テスト1件追加（`npx vitest run`70件・`npx tsc --noEmit`・`npm run lint`・`npm run build`すべて通過）。他に「全クラス」と他表記が混在する科目は`HSP301z`/`HSP401z`の2つだけと確認済み（`class_assignment_filled.csv`全件を機械的に走査）

10. **「全クラス」しか無い演習系科目（英語演習・独語演習・独語運用演習・中国語演習等）でも曜日時限を出すようにした（2026-09-06、開発者からの追加指摘：「英語演習、独語演習とか演習系もない」）**：これらの科目は複数セクションが全部「全クラス」（誰でも自由に選んで受講してよい）で、時限が食い違うため9番修正後も表示されないままだった。「もとより複数あるやつ（で誰でも受けれるやつ）は複数の時限書いていい」という開発者の方針に従い、`resolveSlotsForProfile`のフォールバック（「全クラス」しか候補が無いケース）では、時限が食い違っても`undefined`にせず**候補を重複排除してすべて列挙する**ように変更（例：`ENG302z`前学期なら「月4限/木1限/金4限」のように表示される）。一方、クラス指定で一致した側（優先される分岐）は従来通り、食い違えば`undefined`のまま（本来一意に決まるはずのものが決まらないケースなので、非表示を優先する）。単体テスト1件追加し、既存の1件（HSP301zで該当クラスが無い場合の期待値）も「undefined」から「全クラス2件の列挙」に修正（`npx vitest run`71件・`npx tsc --noEmit`・`npm run lint`・`npm run build`すべて通過）。念のため、学期をまたいで時限が違う科目（`MTH301f`等の確率統計、前学期は通常セクション・後学期は再履セクション）が誤って混在表示されないか確認したところ、これらは全て具体的なクラス指定（I1〜I6クラス・再履全員）で解決される科目で、今回のフォールバックには掛からないことを確認済み

11. **`ENG302z`（英語演習）が後学期の推奨一覧・残りの必修に出ない不具合を修正（2026-09-06、開発者が「後学期の英語演習入れるの忘れてたな」と指摘）**：調べたところ、`ENG302z`は実際のシラバスでは前学期・後学期どちらにも開講されている（他の外国語のように3年次/4年次で別コード「演習」/「運用演習」に分かれておらず、1つのコードで両学期に対応するため）のに、`data/subjects/youran-2025.json`の`termType`が「前学期」のみに固定されていた。原因は`scripts/gen_data.py`の`add()`がコード末尾の数字（3）から前学期/後学期を自動推定するだけの簡易ロジックで、この科目のように実際は学期を問わず開講される例外は考慮していなかったこと（他の言語演習系科目11件は全て実際のofferingsの学期とtermTypeが一致することを確認済み。この科目だけの例外）。`src/domain/recommend.ts`の`isOfferedIn()`は`termType`が設定されているとその学期以外を候補から除外するため、後学期表示時に「残りの必修」「選択科目」からこの科目が消えていた。`gen_data.py`に`SUBJECTS["ENG302z"]["termType"] = None`という上書きを追加（次回`gen_data.py`実行時にも反映される）、`data/subjects/youran-2025.json`のENG302zエントリも同じ内容に直接修正（`gen_data.py`を今実行するとofferingsが消えるため、直接編集で即時反映）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（71件）・`npm run lint`・`npm run build`すべて通過

35. **Ⅱ類・Ⅲ類の2年次「複素関数論」「離散数学」「波動と光」が2年前期のはずなのに後期表示になる不具合を修正（2026-09-06、開発者が「テストケース2にて離散数学と波動と光、複素関数論は2年前期のはずなのに後期で表示されている」と報告）**：`data/subjects/youran-2025.json`の該当16科目コード（`MTH302i/j/k/m/n/p/r`＝複素関数論のⅡ類2件・Ⅲ類5件、`MTH303f/g/h`＝離散数学のⅡ類3件、`MTH304f/g/h`＝複素関数論のⅡ類3件、`PHY302f/g/h`＝波動と光のⅡ類3件）は`standardSemester: 4`・`termType: "後学期"`だったが、これらの`offerings`（2025・2026年度とも実際にシラバス取得済みのデータ）は一貫して`前学期`しか無く、食い違っていた。特に複素関数論の該当コードは今回のセッション序盤で教員・時限を入念に検証済み（開発者の直接の知識で古川/遠藤先生ではなく南/龍野/宮脇/小木曽/鈴木先生の講義だと確定済み）だったにもかかわらず、`standardSemester`/`termType`側の修正が漏れていたことが原因と判明（`offerings`の内容と`standardSemester`/`termType`は`gen_data.py`上は別々のフィールドで、前者だけ直していた）。`scripts/gen_data.py`の該当16行を`standardSemester: 3`・`termType: "前学期"`に修正し、`data/subjects/youran-2025.json`も同じ内容に直接修正（offerings保持のため）。`python scripts/gen_data.py`を一時的に実行して出力の該当フィールドが修正内容と一致することを確認した上で、offerings付きのバックアップに戻した。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`すべて通過

36. **Ⅲ類「波動と光」（`PHY402m/n/p/r`）が3年次表示になっていたのを2年次に修正、かつ「401/402番台」の類専門必修科目全般に同種の疑いがあると判明（2026-09-06、開発者が「波動と光に3年時なんてないはず」と指摘）**：調べたところ`PHY402m/n/p/r`は`standardYear: 3`・`standardSemester: 5`・`前学期`だったが、`offerings`は一貫して`後学期`。さらに、卒業研究着手審査（別表4、`reviews`データ、35番と同じくCLAUDE.mdの[[uec_project_progress]]で単位合計を検証済み）の「2年次までの類共通基礎科目及び類専門科目の必修科目のうち◯単位以上」という条件に、この`PHY402n`等がそのまま名指しで含まれていることを確認。開発者の直接の指摘とあわせ、`PHY402m/n/p/r`を`standardYear: 2`・`standardSemester: 4`・`後学期`に修正（`scripts/gen_data.py`・`data/subjects/youran-2025.json`とも。手順は35番と同様、`python scripts/gen_data.py`で出力一致を確認してからoffering付きに戻した）
    - **重大な追加の疑い**：上記と全く同じ調査手法で、Ⅲ類5プログラム共通の「401/402番台」の類専門必修科目（`ELE401`基礎電子回路・`ELE402`理工学基礎実験・`PHY401`電磁気学および演習・`BIO401`分子生物学・`CHM401`基礎物理化学・`CHM402`無機化学・`COM401`プログラミング演習・`MCE401〜403`など）も軒並み同じパターン（`standardYear: 3`なのに別表4の「2年次までの必修科目」リストに名指しで含まれ、かつ`offerings`が標準学期と逆）が見つかった。開発者に確認したところ「科目によって違う／わからないので個別に確認したい」との回答だった
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`すべて通過

37. **`PDF/tanni_extract_final.pdf`が実は健在だったと判明（2026-09-06）**：[[uec_youran_pdf_references]]に「開発者のPCから無くなった」と記録していたが誤りで、実際はプロジェクト内`PDF/`サブフォルダ（`.gitignore`の`*.pdf`で引き続き除外）にそのまま存在していた（開発者が指摘）。以後はこのフルカリキュラム表PDFを直接参照できる。物理ページ番号は[[uec_youran_pdf_references]]記載の目安（Ⅱ類5プログラム＝32〜41ページ等）がそのまま使える

38. **標準年次・学期のズレをⅡ類「情報通信工学プログラム」で1件ずつPDF照合し、7件を修正（2026-09-06、開発者から「情報通信プログラムのやつだけ」個別確認の依頼）**：36番で見つかった「401/402番台だけでなく広範囲に標準年次・学期のズレがある疑い」について、`PDF/tanni_extract_final.pdf`のC.3.2②情報通信工学プログラム表（114〜115ページ）を直接読んで、`offerings`と食い違っていた科目を1件ずつ裏取りした。結果：
    - **データが間違っていた7件**（PDFの列位置がoffringsと一致する方に修正）：`COM301g`数値解析およびプログラミング演習（2年後期→2年前期）、`COM401g`アルゴリズムとデータ構造およびプログラミング演習（3年前期→2年後期、年次も間違っていた）、`ELE301g`基礎電気回路（2年後期→2年前期）、`ELE801g`/`ELE801h`通信法規（4年前期→4年後期）、`LAB501g`GLTPラボワーク（3年後期→3年前期）、`MTH501g`幾何学概論（3年後期→3年前期）
    - **データはPDF通り正しく、`offerings`側がズレていただけの2件（変更していない）**：`PHY204g`力学（2年前期のまま）、`PHY502g`量子と情報（3年後期のまま）。この2件は「offeringsと標準学期が食い違う＝データが間違っている」と機械的に判断してはいけない実例（36番で使った検出スクリプトの誤検出があり得ることの裏付け）
    - `ELE801h`は電子情報学プログラム(h)の科目だが、`ELE801g`と同一講義（同じ科目名・同じtimetableCode想定）のため一緒に修正した
    - `scripts/gen_data.py`・`data/subjects/youran-2025.json`とも修正（手順は35・36番と同様）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`すべて通過
    - **教訓**：`offerings`と標準学期の食い違いは、標準学期側のバグのこともあれば`offerings`側（シラバス取得年度のズレ・大学側の登録ミスなど）のこともあり、機械的な一括修正はできない。`PDF/tanni_extract_final.pdf`で1件ずつ裏取りするのが確実
    - **残作業**：36番で見つかった他4プログラム（セキュリティ情報学・電子情報学・計測制御システム・先端ロボティクス）およびⅢ類5プログラムの同様の疑いはまだ手つかず。次回、開発者に確認しながら同じ手順で進める

39. **「全てのプログラムにそういうバグがないか洗い出して」との指示を受け、残り10課程（Ⅱ類4・Ⅲ類5・夜間主1）を全てPDF照合して修正完了（2026-09-06〜07）**：38番の手法（`PDF/tanni_extract_final.pdf`の該当プログラム表を画像化し、ヘッダー行と対象行を同じx座標で重ねて列位置を確定する「combo」方式）を全プログラムに展開した。まず`data/subjects/youran-2025.json`の`offerings`と`standardSemester`/`termType`の食い違いを機械的に全16課程分洗い出すスクリプトを書き、Ⅰ類5プログラムは0件（既存データがそもそも正確だった）と確認した上で、残りを1課程ずつ処理：
    - **セキュリティ情報学(f)**：15件修正。選択科目（COM500〜600番台）がほぼ丸ごと1学期分ずれていた
    - **電子情報学(h)**：7件修正
    - **計測・制御システム(i)**：31件修正。機械系科目群（MCE）がほぼ丸ごとずれており、かつ「離散数学」(MTH304i)・「波動と光」(PHY303i)は`offerings`の学期がたまたま一致していたため機械検出をすり抜けていた**年次のみのバグ**も発見（PDF直接確認でしか見つけられない類）
    - **先端ロボティクス(j)**：30件修正（同様のパターン、MTH304j/PHY303jも同じ年次バグ）
    - **機械システム(k)**：19件修正
    - **電子工学(m)**：29件修正
    - **光工学(n)**：34件修正
    - **物理工学(p)**：28件修正
    - **化学生命工学(r)**：29件修正
    - **夜間主**：5件修正（COM701t・ELE701t・GSE701t・INS701t・MCE701t、いずれも3年後期→4年前期）
    - **発見した法則性**：Ⅲ類5プログラム（k/m/n/p/r）で見つかった全ての不具合は、例外なく「`standardSemester`が正しい値より1大きい」（`standardYear`は`(standardSemester+1)//2`で再計算し直せば自動的に合う）という完全に一様なパターンだった。これは付録Cの表を転記する際に列を1つ右にずれて読んでしまった、という単純な転記ミスが型プログラム全体に及んでいたことを示唆する。Ⅱ類（f/g/h/i/j）は必ずしもこの一様パターンではなく（`ELE301i`のように現状データが正しくPDFと一致するのに`offerings`だけがズレている例、`GSE401j`のように元から正しく登録されていた例が複数あった）、プログラムごとに実際にPDFを開いて1行ずつ確認する必要があった
    - **確認できた「offeringsは間違っているがdataは正しい」既知の例外**：`PHY204g`/`PHY204h`（力学）、`PHY502g`/`PHY502h`（量子と情報／基礎量子工学系）、`MTH502f`（幾何学概論）、`ELE301i`（基礎電気回路）、`GSE401j`（機械計測工学）。これらは`offerings`（シラバス取得データ）側が何らかの理由（今年度の開講タイミングの実際のズレ、シラバスシステム側の事情など）でPDFの標準学期と食い違っているだけで、`standardSemester`/`standardYear`は変更していない
    - 全課程で`python scripts/gen_data.py`実行結果と手動修正後の`data/subjects/youran-2025.json`が一致することを確認（offerings保持のため直接編集→gen_data.py側も同じ内容に反映→一時実行して出力比較→offerings付きに復元、という35番以来の手順を毎回徹底）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（96件）・`npm run lint`・`npm run build`を全課程の修正ごとに実行し、すべて通過を確認
    - **`PDF/tanni_extract_final.pdf`の所在が判明**（37番の続報）：プロジェクト内`PDF/`フォルダに`tanni_extract_final.pdf`（本編、これまで通り）に加え`A2.pdf`〜`A4.pdf`（時間割）も格納されていることを確認。以後はこのフォルダを最初に確認すること
    - **残作業**：`offerings`自体が実際の開講タイミングとズレている可能性がある6件（PHY204f/g/h、PHY502g/h、MTH502f、ELE301i）は、シラバス取得データ側の問題である可能性があるが、今回はPDF基準（standardSemester側）を正としたため未調査のまま。必要になれば`fetch_syllabus.py`のシラバス取得結果を個別に再確認する
    - **修正漏れの発見・対応**：全課程の修正完了後に食い違い検出スクリプトを再実行したところ、`netinfo`の`PHY301g`（基礎電磁気学）を修正し忘れていたことが判明（`PHY301h`等の並びのプログラムは直したのに`g`だけ抜けていた）。`(2,4,後学期)→(2,3,前学期)`に修正し、同じ手順（gen_data.py出力確認→offerings付き復元）で反映。これで機械検出上の残存食い違いは、上記の「offeringsが正当な理由でズレている」既知の例外（PHY204系・PHY502系・MTH502f・ELE301i・COM504fの夏ターム表記）のみになった

40. **残タスク一覧③（未実装の機能）を全項目実装（2026-09-07、開発者が「シャワー浴びてくるから3をなるべくやっておいて」と依頼、自律実行）**：
    - **サイト名変更**：`index.html`の`<title>`・`TopPage.tsx`の`<h1>`を「電通大 単位取得ルートナビ(β版)」に変更
    - **最終更新日の表示**：`vite.config.ts`に`define: { __BUILD_DATE__: ... }`を追加し、ビルド時点の日付（`YYYY-MM-DD`）をコードに埋め込む方式にした。GitHub Actionsはmainへのpushのたびにビルドし直すので、これがそのまま公開サイトの最終更新日になる。型は`src/vite-env.d.ts`（新規）で`declare const __BUILD_DATE__: string`と宣言。TopPageの見出しの横に表示
    - **科目一覧・詳細ページ（F-5）を実装**：`CoursesPage.tsx`（プレースホルダーだった）をキーワード・学期・曜日で絞り込める一覧に、`CourseDetailPage.tsx`を開講情報（シラバスリンク付き）・先修科目・「要件上の位置づけ」を表示する詳細画面にした。`src/data/requirementSets.ts`に`findSubjectUsages(code)`を新規追加（全16プログラムの`groups`木を再帰的にたどり、指定した科目番号がどのプログラムのどの区分＝親グループ名の連なりに属するかを集めて返す）。ルーティング自体（`/courses`・`/courses/:id`）は既にApp.tsxに配線済みだった（過去のフェーズでプレースホルダーとして用意されていたもの）。Playwrightで実際に「複素関数論」を検索→詳細表示→39番で修正したばかりの正しい教員・時限（宮脇陽一先生・水3限等）が表示されることを確認済み（データ修正とページ実装の両方が正しく連動していることの裏付けにもなった）
    - **科目名をシラバスへのリンクに**：`MainPage.tsx`に`nameLink(code)`ヘルパーを追加し、既存の6箇所の`{nameOf(code)}`表示をすべて置き換えた。`GroupProgress`は`MainPageContent`とは別のトップレベル関数なので、`nameLink`も`intensiveSeasonOf`等と同様にpropとして明示的に渡す必要があった。複数セクションでシラバスURLがバラバラな科目・offeringsが無い科目は「代表URLを決められない」としてリンクにせず名前だけ表示する（誤ったリンクを出すよりは安全側に倒した）。Playwrightで188件のリンクが正しいシラバスURL・`rel="noopener noreferrer"`付きで生成されることを確認
    - **その他単位認定のJSON書き出し・読み込み対応**：`src/domain/importers.ts`の`ExportedData`/`ImportResult`に`otherCommonCredits?: number`を追加し、`schemaVersion`を2に更新（`CURRENT_SCHEMA_VERSION`としてexport）。読み込み側は`SUPPORTED_SCHEMA_VERSIONS = [1, 2]`として、フィールドが増えただけの旧バージョン（1）もそのまま読める設計にした（無ければ`undefined`、単に「ファイルに記載が無かった」として扱う）。`MainPage.tsx`の読み込み処理で、ファイルに記載があればその値でその他単位認定を置き換える（科目コードを持たない単一の数値なので、recordsのような「追加・更新」の概念は無く、単純に上書き）。単体テスト2件追加
    - `npx tsc --noEmit`・`npx vitest run`（98件）・`npm run lint`・`npm run build`をこまめに実行しすべて通過。新規実装した3項目（F-5・科目名リンク・上記のPlaywright確認）はブラウザでも動作確認済み。その他単位認定のexport/import往復までは（ファイルダウンロード操作を伴うため）ブラウザ確認を省略し、型チェック＋単体テストのみで済ませた
    - 全てdevブランチにコミット・push済み（4コミットに分割：サイト名/最終更新日+F-5、科目名リンク、その他単位認定export/import、このログ更新）

41. **「応用数学A/B・基礎演習A/Bの時限が表示されない」報告をきっかけに、シラバス・データ全体の見直し（27番の残タスク）を実施（2026-09-07、開発者が「シャワー浴びてくるから」「もう寝るから」と就寝中に自律実行、範囲は「2026の電通大シラバス全てと現在のデータ全て」と明示指示）**：
    - **原因調査**：`応用数学A`/`応用数学B`/`基礎演習A`/`基礎演習B`（Ⅱ類5プログラム分、計19科目コード）は`offerings`が0件だった。実際のシラバス一覧を確認すると、科目名が「応用数学Ａ（Ｉエリア）」のように**全角英字＋末尾のエリア表記**になっており、`fetch_syllabus.py`の名前突き合わせ（全角半角変換・末尾（…）除去は既存の`normalize_for_match`/`strip_class_suffix`で対応済みのはずだった）が実際には機能していなかった。Pythonで直接照合ロジックを再現したところ**現在のスクリプトのロジックなら正しくマッチする**ことを確認、つまりコードは既に正しいが、この修正が入って以降`fetch_syllabus.py`が一度も再実行されていなかっただけと判明
    - **フルスクリプト再実行前の安全対策**：`fetch_syllabus.py`を再実行すると、これまでの個別バグ修正のうち**スクリプト側に反映されておらずJSONへの直接パッチだけで対応していたもの**（複素関数論Ⅰ・Ⅱ類9科目コード、30番参照）が失われるおそれがあった。大学のシラバスシステム自体の登録ミス（古川怜先生・遠藤晋平先生の講義ページに、本来無関係なⅠ・Ⅱ類の科目コードが誤って登録されたまま）が生きている限り、自動マッチングは間違った方（Ⅲ類向け）を拾い直してしまうため。他の既知の手動修正（ENG101/102/201/202のs/z入れ替え、UEC001z/002z、CAR603z/604z）は全て`fetch_syllabus.py`内に恒久的な補正コードとして既に存在することを確認済みだったので、**複素関数論の9科目コード分だけ**、同じ形式（`offerings_by_code[code] = [...]`による無条件上書き）の恒久補正を新規に追加した
    - **フル再実行の結果**：`FACULTIES = ["31", "32"]`両方で1102＋83候補行を処理し、offerings付与1110科目（前回1046→1110）・prerequisitesText付与939科目。再実行前後で**offeringsを完全に失った科目は0件**、既存の学期情報が食い違う形に変化した科目も0件、**新たにofferingsを獲得した科目45件**（応用数学A/B・基礎演習A/Bの19件＋COM402f・COM502e・COM504e・COM701e・ELE403h・ELE502g/h・ELE601g・ELE602g/h・ELE603h・INT002c/d/g/h・LAB701t/702t・MCE502i/503i/503j/504j/601i/601j/602i/602j/602k）。複素関数論9科目コードの保護も正しく機能していることを確認
    - **`class_assignment`の監査・更新**：`scripts/build_class_assignment.py`を再実行し、新しいofferingsを反映した`class_assignment.csv`（1626行）を生成。これと`class_assignment_filled.csv`（開発者の手作業データ）を突き合わせ、「まとめ書き記法（`3，4`のような複数時限を1行にまとめる書き方）」を展開してから比較する必要があると気づいた上で、真の孤立行（現在のofferingsのどのキーにも一致しない行）を検出：**6行のみ**（`COM403i/j`・`COM402m/n/p/r`＝「計算機工学」。17番で「木2→金1」に手動修正した科目だが、単一セクションに整理され`build_class_assignment.py`の対象外＝もう`class_assignment`を経由しなくてもMainPageで直接表示できる状態になっていたため、残っていたのは単なる不要データだった。実際の時限は変わっていない）。手動記入済みの重要なパターン（「一類/二類/三類」「Ⅲ-N」「再履生/再履全員」等）はすべて正しく引き継がれていることも個別に確認した上で、`class_assignment_filled.csv`を`class_assignment.csv`の内容でまるごと置き換え（重複ロジックを避けるため。差分は上記6行の除去のみ）、`scripts/build_class_assignment_json.py`で`class_assignment.json`を再生成（1509件）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（98件）・`npm run lint`・`npm run build`すべて通過。Playwrightで、既存の複雑なデータ（複素関数論・Technical Englishの複数時限列挙）が壊れていないこと、新規追加科目（応用数学A等）は「class_id未記入なので時限非表示」という安全な状態（誤表示ではない）になっていることを確認済み
    - **開発者への申し送り**：新たに複数セクション扱いになった科目のうち`class_id`が空欄のまま（112行）を`Downloads\class_id_worksheet_20260907.csv`に切り出し済み。時間割PDFを見て`class_id`列を埋めて返してもらえれば、`class_assignment_filled.csv`にマージして`class_assignment.json`を再生成する
    - **今回のスコープで見つからなかったもの**：offeringsが完全に0件のまま残る科目（大学院連携科目・卒業研究・輪講など、性質上シラバス一覧に出ない科目が大半と推測されるが、全件の内訳までは今回確認していない）は未調査のまま。「シラバス全体の見直し」はこれで一区切りだが、完全に尽くしたわけではない

42. **留学生・転類生・転プログラム対応の方針を決定し実装（2026-09-07、開発者の依頼）**：
    - **留学生**：特に対応しない方針に決定（プロフィール項目は追加しない）。`classIdMatchesProfile`が`留学生`表記に対してfalseを返す＝該当科目の曜日時限が表示されないだけ、という既存の挙動のまま
    - **転類・転プログラム**：開発者から「転類したか・転プログラムしたかのチェックボックスを作り、選んだ場合は特殊な入力になる」という設計案が出て、詳細を詰めた結果、以下の形で実装した
      - **前提**：転類は1年次→2年次、転プログラムは2年次→3年次のタイミングでしか起こらないという大学の制度上の制約（開発者確認）を前提にする。「いつ転属したか」は記録せず、「元の類・プログラム・クラス」だけを記録する（当初「転属時期も記録」という案が一度出たが、開発者が「元のクラスとか類だけでいい」と訂正）
      - `src/storage/profile.ts`の`Profile`に、`transferredCluster`（転類したか）・`previousCluster`（元の類）・`previousYearOneClass`（元の1年次クラス）・`transferredProgram`（転プログラムしたか）・`previousProgramCluster`（元の類）・`previousProgram`（元のプログラム）・`previousClassIABC`/`previousClassIIArea`/`previousClassIIIYear2Class`（元のクラス、元の類に応じて1つだけ使う）を追加
      - `SetupPage.tsx`に、チェックボックスと条件付きで出てくる選択欄を追加（既存の「現在の類・プログラム・クラス」の入力パターンをそのまま「元の」バージョンとして再利用する形）。元の類を選び直すと、それまで選んでいた元のプログラムはリセットする
      - **曜日時限の判定ロジック（`classIdMatchesProfile`）には配線していない**：`class_assignment.json`側に「転類生」「転プログラム生」に対応する`class_id`表記の実例が今のところ0件（8番参照）なので、拡張する材料が無い。実際にそうした表記が見つかったときに配線する
    - Playwrightで、チェックボックスのon/offで選択欄の表示が切り替わること、元の類を変えると元のプログラムの選択肢が正しく絞り込まれること、保存した値がlocalStorageに正しい形で残ることを確認済み
    - `npx tsc --noEmit`・`npx vitest run`（98件）・`npm run lint`・`npm run build`すべて通過
    - **卒業要件の判定ロジックへの配線は43番で別途対応**（プロフィールへの保存だけでは、転属前に修得した科目を実際に登録する手段が無いままだったため）

43. **転類・転プログラム前に必修だった科目を登録できる「その他の科目」欄を追加（2026-09-07、開発者から「転類、転プログラムする前に履修した科目も登録できないと不便」との追加依頼）**：
    - **要件**：元の類・プログラムでは必修だったが、今の類・プログラムの卒業要件には出てこない科目を一覧で出し、修得にできるようにする。転類は1年次科目、転プログラムは2年次科目が対象（42番の前提通り）。類をまたぐ転プログラムで生じる「他類の専門科目」は対象外（開発者の指示、同じ類内の転プログラムなら大抵は付録C注1の「他プログラム専門科目」展開で既にカバーされているとのこと）。修得にした科目は、同名の科目でない限り共通単位になる
    - **設計**：`src/data/requirementSets.ts`に`getTransferBucketSubjects(entryYear, oldCluster, oldProgram, yearLevel, currentSet)`を追加。元の類・プログラムの要件セットから`kind:'required'`の科目コードを集め、(a)今の要件セットに同じコードがそのまま出てくるもの（理数基礎科目など元々共通のもの）、(b)今の要件セットのどこかに**同じ科目名**の科目があるもの、の両方を除外する。(b)が実質「同名の科目でない限り」のロジックで、除外されなかった科目＝今の要件と重複が無い科目だけが一覧に残る。転類（元のプログラムが分からない）は元の類の最初のプログラムを代表として使う（1年次の必修科目は同じ類ならプログラムが違っても名前・単位数が共通のはずという前提）
    - `MainPage.tsx`に「その他の科目（転類・転プログラム前に必修だった科目）」セクションを追加（`transferredCluster`/`transferredProgram`のときだけ、対象科目が1件以上あれば表示）。科目コードは既存の`committed`/`draft`のMapにそのまま追加する形にしたので、状態管理・保存・「更新」「リセット」ボタンの動作は既存の仕組みがそのまま使える（特別な配線が不要だった）。修得にした分の単位は、その他単位認定（31番）と同じ`otherCommonCredits`の引数にまとめて足し込むことで、共通単位の上限処理を含めて既存のロジックをそのまま再利用した
    - **今回のテストで見つかった副産物のデータ修正**：機能確認のため実際にⅢ類mecha→Ⅰ類mediaの転類シナリオを試したところ、`力学演習`(PHY205k)が一覧に出てこないことに気付いた。調べると`standardYear`が2のままで（41番で`力学`のPHY204kは直したがPHY205kが漏れていた）、別表4の記載（「力学・力学演習は1年次開講の必修科目2科目」）とも矛盾していたため、`standardYear: 1`・`standardSemester: 2`に修正（`scripts/gen_data.py`・`data/subjects/youran-2025.json`とも）
    - Playwrightで、転類（Ⅲ類mecha→Ⅰ類media）で「力学」「力学演習」が一覧に出ること、修得にして更新すると共通単位が実際に増えること（0→2単位）、転プログラム（Ⅲ類mecha→Ⅲ類electro）で「材料力学および演習」「機械力学および演習」が一覧に出ることを確認済み。コンソールエラー無し
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（98件）・`npm run lint`・`npm run build`すべて通過

44. **`Downloads\class_id_worksheet_20260907.csv`の記入分をマージ（2026-09-07、開発者が記入して返却）**：112行中52行が記入済みだった。うち30行（`GSE301f`・`MTH302f/g/h`・`MTH303i/j`の「応用数学A」「基礎演習A」、Ⅱ類のI1〜I6エリア別）をそのまま`class_assignment_filled.csv`にマージ。マージにあたり新しい省略記法を発見・対応：
    - **「I1,I2クラス」のような書き方**：「クラス7，8」（数字が後）とは逆で、`I1`〜`I6`の番号が先に並んで最後に1回だけ「クラス」が付く書き方。`scripts/build_class_assignment_json.py`に`expand_iarea_shorthand()`を追加して「I1クラス,I2クラス」に展開するようにした
    - **`GSE401h`（基礎演習B、電子情報学）の2行**：開発者コメントが「重複、いらない消していい」だったため、マージせずそのまま除外した
    - **`MCE602i/j/k`（マシンデザインB）の4行**：開発者コメントが「計測・制御システムプログラム(こっちは全体)、機械システムプログラムの一部(振り分けは未定)」だったため、確定している方のプログラム名だけを`class_id`にし（火曜=計測・制御システムプログラム、水曜=先端ロボティクスプログラム）、機械システムプログラムの一部も対象だが振り分け未定という旨を備考に残した
    - **`ELE402g`/`GSE401g`（基礎情報通信・基礎演習B）の4行、`MTH401f/g/h/i/j`（応用数学B）の10行は保留**：「情報通信工学プログラム、電気情報プログラムの学籍番号偶数」のような、複数プログラム名＋学籍番号偶数/奇数を組み合わせた新しい複合記法で、既存の「読点区切り＝OR条件の一覧」という分割ルールとぶつかる（同じ読点が「プログラムの列挙」と「複数候補の区切り」の両方の意味で使われてしまう）。誤った判定ロジックを組むリスクが高いため、今回はマージせず開発者に記法を確認してから対応する方針にした
    - `scripts/build_class_assignment_json.py`で`class_assignment.json`を再生成。マージ処理のバグで、キーが一致する既存の空欄行を残したまま新しい記入済み行を追加してしまい24件の重複が発生していたことに後で気づき、別途除去した（`GSE401h`の2行も、開発者の「消していい」指示通りこのタイミングで実際に削除）。Playwrightで、Ⅱ類security・classIIArea別（I1/I3/M）で「基礎演習A」の時限が正しく出し分けられることを確認済み
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（98件）・`npm run lint`・`npm run build`すべて通過

45. **保留していた「プログラムA、プログラムBの学籍番号偶数/奇数」複合記法に対応（2026-09-07、開発者に意味を確認：「両方のプログラム共通で、学籍番号偶数の人だけ対象」）**：
    - `scripts/build_class_assignment_json.py`に`expand_parity_programs()`を追加。既知のプログラム正式名称の一覧（`PROGRAM_NAMES`）を使い、「プログラムA、プログラムBの学籍番号偶数」のような並びを検出して、読点区切りではなく「＆」区切りの`プログラムA＆プログラムBの学籍番号偶数`という単一トークンに変換してから渡すようにした（読点は他の箇所で「複数候補の一覧（OR）」の区切りに使っているため、そのまま渡すと壊れてしまうのを避けるため）。あわせて開発者の手書きにあった表記ゆれ（「電気情報プログラム」→正式名称「電子情報学プログラム」、「計測・制御システムプロフラム」→「計測・制御システムプログラム」）を補正する`PROGRAM_NAME_TYPOS`辞書も追加
    - `MTH401f/g/h/i/j`の「…の学籍番号奇数と先端ロボティクスプログラム」のような、学籍番号条件付きの一覧のうしろに無条件のプログラムがさらに「と」で繋がるケースにも対応（`PARITY_AND_PROGRAM_RE`で「偶数と」「奇数と」の「と」を先にカンマへ変換してから上記の変換をかけることで、「(プログラムA＆プログラムBの学籍番号奇数) OR (プログラムC)」という意味になる）
    - `src/domain/classAssignment.ts`の`classIdMatchesProfile`に、`＆`を含む`〜の学籍番号偶数/奇数`パターンの判定を追加（該当プログラムのいずれかに所属していて、かつ学籍番号（1年次クラスの偶奇から導出）が一致するときだけtrue）。単体テスト1件追加
    - 保留していた`ELE402g`・`GSE401g`・`MTH401f/g/h/i/j`の計14行を`class_assignment_filled.csv`にマージ（ワークシートを一度上書きしてしまい元の記入内容を一時的に見失ったが、会話履歴に残っていた読み取り結果から復元した）
    - Playwrightで、`MTH401f`（応用数学B）についてnetinfo/electroinfoの偶数・奇数、robotics（無条件）、control（無条件）の組み合わせすべてで正しい時限（木・2限／月・3限）が出ることを確認。securityは今回のデータに含まれておらず非表示のままだが、これは仕様通り（誤表示より非表示を優先）
    - `python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（99件）・`npm run lint`・`npm run build`すべて通過

46. **「2限分ある科目は時限をくっつけたから無いし計算機工学はその時限のシラバス存在しないし情報通信と符号化なんて科目は存在しないの！」との報告を受け、3件のデータ不具合を調査・修正（2026-09-07）**：
    - **基礎科学実験A1/A2/B1/B2（`PHY101z`/`PHY201z`/`CHM101z`/`CHM202z`）の記入済みclass_idが消えていた根本原因を特定**：`scripts/build_class_assignment.py`の`find_existing()`が、記入済みCSVの`period`列をそのまま文字列キー（例:`"3，4"`）として引き継ぎ判定に使っていたため、41番のシラバス再取得で`build_class_assignment.py`を再実行した際、offerings側は1スロット1時限（`period="3"`・`period="4"`と個別）で照会するのに対しキーが一致せず、28番で開発者がまとめ書きした4科目・26行ぶんの記入内容がsilently失われ、`class_schedule.csv`との別系統の自動マッチング（`A1-N`形式）による中途半端な部分解決に置き換わっていた（気付かれないまま65a6323のコミットに含まれてしまっていた）。`build_class_assignment.py`に`split_periods()`を追加し、`existing_by_key`を構築する際にまとめ書きの時限を個別に展開してキーを作るよう修正（今後同様のまとめ書きを再実行しても消えなくなる）。失われていた元の記入内容は、消える前のコミット（`0f26453`）から`git show`で復元し、`class_assignment_filled.csv`に書き戻した
    - **`COM402k`（計算機工学、機械システムプログラム向け）に誤った時限が付いていた不具合を修正**：2026年度シラバスを直接確認したところ、この時限（木曜2限、timetableCode 21322216）は実際には「プログラミング演習（クラスA）」という全く別の講義のページで、その科目番号欄に無関係な`COM402k`が誤って混入していたことが判明（大学のシラバスWeb公開システム側の登録ミス。同ページの本来の対象は`COM401k/m/n/p/r`）。一方、正しい「計算機工学（Ⅲ 類）」ページ（timetableCode 21322218、遠藤幸一先生、金曜1限）の科目番号欄には`COM402m/n/p/r`のみが登録されており、`COM402k`はどちらにも正しく載っていない＝**2026年度シラバスにはCOM402kの正しい登録が見当たらない**（進捗ログ17番で「機械システムプログラム向けの別講義（遠藤エミ／松本俊哉担当）」と記録されていたが、今年度は同名での独立ページが見つからなかった）。誤ったデータより「非表示」の方が安全なため、`data/subjects/youran-2025.json`のCOM402kから該当offerings・prerequisitesTextを削除し、`fetch_syllabus.py`に`IGNORE_CODE_MATCH`（(timetableCode, 科目コード)の組み合わせを明示的に無視するしくみ）を追加して次回再取得時も同じ誤りを拾わないようにした。`class_assignment_filled.csv`の該当2行（未記入だった）も削除
    - **`ELE403h`（情報通信と符号化）は「存在しない」のではなく、現在は`ELE402g`と合同の「基礎情報通信」という講義として開講されていることを確認**：2026年度シラバスで該当する2つのtimetableCode（21224207・21224282、藤井威生先生、月2限/水3限）の科目番号欄はどちらも`"ELE402g ELE403h"`と両方のコードを一貫して明記しており、大学側の登録ミスではなく意図的な合同開講と判断した。時限・担当教員データ自体は正当なので変更していない。**学修要覧の正式名称「情報通信と符号化」と、現在の合同講義の呼称「基礎情報通信」が食い違っている**点は開発者に確認が必要な事項として残す（`gen_data.py`側の科目名を「基礎情報通信」に合わせるべきかどうかは未確認のまま）
    - `scripts/build_class_assignment.py`・`scripts/build_class_assignment_json.py`を再実行し、`data/timetable/class_assignment.csv`・`class_assignment.json`を再生成。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（99件）・`npm run lint`・`npm run build`すべて通過。Playwrightで、Ⅱ 類情報通信工学プログラム・1年次クラス7の基礎科学実験4科目が「月・3限/月・4限」と正しく表示されること、Ⅲ 類機械システムプログラムの計算機工学（COM402k）が誤った「木・2限」ではなく非表示になったこと（同じプログラムのプログラミング演習は引き続き正しく「木・2限」と表示される）を確認済み
    - **開発者への申し送り**：ELE403hの科目名を「基礎情報通信」に変更すべきかどうか、COM402kが今年度本当に開講されないのか（休講/隔年開講/別名開講の可能性）は、Claude側では判断がつかないため確認が必要

47. **COM402k・ELE403hの食い違いの原因を確定、ELE403hの科目名を変更（2026-09-07、開発者の推測「学修要覧が2025年度・シラバスが2026年度だから食い違っている、COM402kはもう無いのでは」を受けて検証）**：
    - **COM402k**：`class_assignment_filled.csv`の全コミット履歴を`git log`で遡って確認したところ、この科目は最初にCSVへ追加された時点（コミット`43b1cdc`、進捗ログ17番）から一貫して「結城　宏信／山鹿　汐音」（＝プログラミング演習の教員）のデータしか持っておらず、CLAUDE.md17番に書いた「担当は遠藤エミ／松本俊哉」という記述自体が誤りだった（もっと古い、2025年度シラバスを見ていた頃の記憶が紛れ込んだものと推測）。加えて2026年度シラバス全体を「遠藤　エミ」「松本　俊哉」で検索しても0件で、このお二人は今年度この学域で何も担当していないことを確認。学修要覧2025のカリキュラムにはCOM402k（機械システムプログラム向け計算機工学）が要件として載っているが、2026年度の実開講にはこの独立枠が見当たらない、という開発者の推測を支持する材料が揃った（隔年休講か完全な廃止/合併かまでは特定できず、データは46番の通り「非表示」のまま）
    - **ELE403h**：開発者の指示により、科目名を学修要覧の「情報通信と符号化」から、2026年度シラバス上の合同講義名「基礎情報通信」に変更することを決定。`scripts/gen_data.py`（該当行にコメント付きで変更理由を明記）・`data/subjects/youran-2025.json`・`data/timetable/class_assignment_filled.csv`のsubject_name列を修正（同じ「情報通信と符号化」という名前を持つ夜間主の`COM502t`は無関係の別科目なので変更していない）。`python scripts/gen_data.py`を一時実行して出力の名前が一致することを確認した上で、offerings付きのバックアップに戻す（35番以来の手順）
    - `scripts/build_class_assignment.py`・`scripts/build_class_assignment_json.py`を再実行。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（99件）・`npm run lint`・`npm run build`すべて通過

48. **「あとなにがある？」の確認中に、46番と同種の別バグを発見・修正（2026-09-07）**：残タスク一覧を最新化するため`class_assignment_filled.csv`の未記入行を機械的に洗い出したところ、`MCE602i/j/k`の記入済み行（火曜=計測・制御システムプログラム、水曜=先端ロボティクスプログラム）が、period列に読点「、」（U+3001）区切りで「3、4」とまとめ書きされていたことが判明。46番で対応した`split_periods()`の区切り文字は半角/全角カンマ（`,`/`，`）のみで読点「、」を含んでいなかったため、この3科目だけ46番の修正が効かず、シラバス再取得のたびに引き継ぎが失敗して空欄の重複行（12行）が生成されたまま残っていた。`split_periods()`の正規表現に読点を追加し、`class_assignment_filled.csv`の空欄重複12行を削除。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（99件）・`npm run lint`・`npm run build`すべて通過

49. **`/compare`（プログラム比較）・`/data`（データ全消去）ページを削除（2026-09-07、開発者が「2は両方消していいよ」と明示許可）**：
    - `ComparePage.tsx`（F-6、未実装のプレースホルダーのまま）・`DataPage.tsx`（「全消去」ボタンのみ。MainPage.tsxの「リセット」ボタンが31番でその他単位認定も含めて上位互換になっていたため完全に重複していた）を削除。`App.tsx`のimport・ナビリンク・ルート定義からも除去
    - `docs/SPEC.md`もあわせて更新：F-6は「→削除」に変更、F-8（データ保存・持ち運び）にダウンロード・読み込み・全消去がMainPageのツールバーに統合され`/data`ページ自体は削除された旨を追記、画面構成の一覧（§6）から`/compare`・`/data`の行を削除、F-6を参照していた2箇所の記述も整合するよう修正
    - `RoutePage.tsx`が使う`PagePlaceholder`コンポーネントは引き続き使われているため削除していない
    - `npx tsc --noEmit`・`npx vitest run`（99件）・`npm run lint`・`npm run build`すべて通過。Playwrightでナビゲーションから「プログラム比較」「データ」のリンクが消えたこと、他のページ（トップ・メイン画面）が引き続き正常に動作しconsoleエラーが出ないことを確認済み

50. **理数基礎(必修)・類共通基礎(必修)等の必修科目がシラバスにリンクできない不具合を修正（2026-09-07、開発者が「回路システム学第一第二、電磁気学第一第二などは飛べるが、理数基礎(必修)類共通基礎（必修）の科目全てが飛べない。英語系は教師を絞り込めないから仕方ない」と報告）**：
    - `MainPage.tsx`の`nameLink`（40番で追加）は「全offeringsのsyllabusUrlが完全一致する科目しかリンクにしない」という単純な判定だったため、回路システム学第一のように少数セクション（プログラムごとに1人ずつ教員がいる）科目は`resolveOfferingsForProfile`（後述）で正しく絞り込めていたが、微分積分学第一・線形代数学第一のような1年次の理数基礎科目（12クラス分・12人の教員がそれぞれ別のシラバスページを持つ）は絞り込みロジック自体にバグがあり、常にリンクにならなかった
    - **根本原因**：`class_assignment.json`の1エントリは`(科目・学期・曜日・時限)`をキーに持つが、**担当教員名を持っていなかった**。1年次の理数基礎科目のように「同じ火曜3限に天野先生→クラス1、齋藤先生→クラス5、大野先生→クラス11、という3人の教員が並行開講している」科目では、`resolveOfferingsForProfile`が「このofferingが実際にどの教員のものか」を区別できず、3つのofferingそれぞれが（本来無関係な）クラス1・5・11すべてを「候補」として拾ってしまい、結果としてクラス5の学生に対しても3人分のURLが候補に残ってurls.size!==1でリンクなしになっていた（`resolveSlotsForProfile`の曜日時限だけを知りたい用途ではこの区別が無くても偶然正しい時限が返っていたため、これまで気づかれていなかった）
    - **修正**：`scripts/build_class_assignment_json.py`が`class_assignment.json`の各エントリに`instructors`（担当教員名の配列）を含めるように変更。`src/domain/classAssignment.ts`の`matchedClassId`に、同じ曜日時限に複数のエントリが並ぶ場合はoffering自身の`instructors`と各エントリの`instructors`が重なるものだけに絞り込む処理を追加（`teacherTokens`/`teacherOverlaps`、`scripts/build_class_assignment.py`の`teacher_tokens`/`teacher_overlaps`と同じ考え方）。offering・entryのどちらかに教員名が無い場合や絞り込んだ結果0件になる場合は、従来通りの（絞り込まない）広い一致に自動的にフォールバックするため、既存の動作（`resolveSlotsForProfile`の全テスト）に影響しないことを確認済み
    - `nameLink`・`dayPeriodTag`で重複していたクラス判定用プロフィールのオブジェクトリテラルを`classProfile`という1つの変数にまとめ、両方から参照するよう整理
    - 単体テスト1件追加（微分積分学第一を模した3教員並行開講のケースで、正しい1件だけが絞り込まれることを確認）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（102件）・`npm run lint`・`npm run build`すべて通過。Playwrightで回路システム学第一・微分積分学第一・線形代数学第一が正しくシラバスにリンクされること、Academic Written Englishのような英語系科目は引き続きリンクにならない（教師を絞り込めないため安全側に倒す、開発者の想定通り）ことを確認済み

51. **「コンピューターネットワーク、データベース論など学年学期が違う」との報告を受け、全16課程の標準年次・学期をPDFと機械照合する仕組みを作り、44件のバグを発見・修正（2026-09-08、開発者が「なるべくやっといて」と就寝中に依頼）**：
    - **新しい検出手法**：これまで（38・39番）は「`offerings`（シラバス取得データ）の学期と`standardSemester`の前後学期が食い違う」ことを手がかりにバグを探していたが、**学期の偶奇（前学期/後学期）が一致していて年次だけがズレているケース**はこの方法では原理的に検出できない（コンピュータネットワーク・データベース論はまさにこのパターンだった）。そこで`PDF/tanni_extract_final.pdf`の毎週授業時間数の表を`pdftotext -layout`でテキスト抽出し、ヘッダー行「1 2 3 4 5 6 7 8」のX座標を較正してから、各科目コードの行にある「２」の印がどの列（＝標準年次・学期）にあるかを機械的に読み取るスクリプトを新規に書いた。Ⅰ・Ⅱ・Ⅲ類15プログラム全体で643科目分を抽出し、`data/subjects/youran-2025.json`の`standardYear`/`standardSemester`と突き合わせたところ131件の食い違いが見つかった
    - **機械抽出の信頼性に濃淡があると判明**：「理数基礎科目」「類共通基礎科目」（`cluster-basic`系）は表の一番上から連続して読み取れるため信頼性が高い一方、「類専門科目」（`major`系、プログラム名の縦書きラベルが長い区分）は`pdftotext -layout`が科目名の文字幅によって数値の位置をわずかにズラすことがあり、`CHM501r`（物理化学第一）で実際に誤検出を確認した（機械抽出は「本来6列目のはず」と言っていたが、画像で直接確認すると実際は5列目で現状データの方が正しかった）。そのため131件を`cluster-basic`系（44件）と`major`系（87件）に分け、**44件（信頼性が高い方）だけを今回まとめて修正**した。全て`PHY204f/g/h/i/j`（力学、1年後学期のはずが2年前学期になっていた）・`MTH301f/g/h/i/j`（確率統計）・`MTH302f/g/h`（応用数学A）・`GSE301f/g/h/i/j`（基礎演習A）・`MTH401f/g/h/i/j`（応用数学B）・`GSE401g/h`（基礎演習B）・`PHY303f/g/h`（力学演習）・`ELE301f/i/k`（基礎電気回路）・`COM301f`・`COM401f/k`・`MTH303i/j`（応用数学A）・`MTH402i/j`（基礎演習B）・`PHY301k`（熱力学）・`PHY302k`（基礎電磁気学および演習）・`ELE401k`（基礎電子回路）・`COM402k`（計算機工学）・`BIO401k`（分子生物学）というⅡ類4課程・Ⅲ類機械システムの「類共通基礎科目」の必修/選択必修科目群で、いずれも標準年次・学期が実際より1〜2学期遅く記録されていた
    - **`COM505f`・`COM506f`（コンピュータネットワーク・データベース論、セキュリティ情報学プログラム、開発者が最初に報告した科目）は画像で直接目視確認した上で個別に修正**：4年前学期(7)と記録されていたが、正しくは3年前学期(5)だった
    - **重大な訂正：`ELE301i`（基礎電気回路、計測・制御システムプログラム）が39番で「offeringsだけがズレていてdata自体は正しい既知の例外」と記録されていたが、これは誤りだったと判明**。今回の直接確認で標準学期が実際は3（二年次前学期）であるべきところ4（二年次後学期）になっていたバグだと分かり、修正した。CLAUDE.mdの残タスク一覧「実害の無い既知の残存事項」からも削除した
    - **残る87件のうち85件（`major`系、信頼性に懸念があるため保留）は`docs/PENDING_YEAR_SEMESTER_CHECKS.md`に切り出した**。画像での目視確認が済んでから修正する方針。特に`FGN`で始まる留学生向け補講科目（基礎数学演習・基礎物理学演習・情報処理演習）は、同じ科目名なのにプログラムごとの期待値がバラバラ（1年〜4年に散らばる）という不自然な結果になっており、「随時」のような特殊な開講時期表記のせいで機械抽出そのものが誤動作している疑いが強いため、優先度を下げるよう注記した
    - `scripts/gen_data.py`・`data/subjects/youran-2025.json`とも修正（手順は35番以降と同様：先にJSONへ直接パッチ→`python scripts/gen_data.py`を一時実行して出力の該当フィールドが一致することを確認→offerings付きのバックアップに戻す）。`python scripts/validate_data.py`・`npx tsc --noEmit`・`npx vitest run`（102件）・`npm run lint`・`npm run build`すべて通過
    - **開発者への申し送り**：`docs/PENDING_YEAR_SEMESTER_CHECKS.md`の85件（`major`系、要目視確認）が残っている。確認方法・注意点は同ファイルに記載済み

52. **科目一覧ページ（`/courses`）のデザインを一新（2026-09-08、開発者が「科目一覧のデザインが気に入らない」「学期だけじゃなくて学年学期まで絞り込めるように」「プログラムで絞れるように、選ばないと専門科目が出ないという注意書きも」「単位の種類を見出しにして下に科目を書く（前に頼んだはず）」と指示）**：
    - `src/data/requirementSets.ts`に`getCourseListSections(entryYear, course, cluster, program)`を新規追加。要件セットの`groups`木を再帰的にたどり、`subjects`を直接持つ末端グループ（＝理数基礎（必修）・類共通基礎（選択）・類専門（選択必修）のような「単位の種類」区分そのもの）を1セクションとして返す。**プログラム未指定のときは共通ファイル（`2025-day-common.json`）の区分だけを返す**仕様にし、「プログラムを選択しないと類専門科目等が出てこない」という開発者の要望をそのままロジックに落とし込んだ
    - `CoursesPage.tsx`を全面書き換え：
      - プログラム選択（`<select>`、Ⅰ類/Ⅱ類/Ⅲ類/夜間主で`<optgroup>`分け）を最上部に配置し、隣に「※プログラムを選択しないと、理数基礎・類共通基礎・類専門科目は表示されません」という注記を追加
      - 学期（前学期/後学期）に加えて学年（1〜4年）の絞り込みを追加（別々の`<select>`2つ、AND条件）
      - 一覧は「単位の種類」区分（`getCourseListSections`が返す各セクション）ごとに`<h2>`見出し＋テーブルで表示。見出しはグループの`label`（無ければ`name`）をそのまま使い、`kind`（必修/選択/選択必修/自由科目/国際科目）がラベルに含まれていなければ「（〜）」で併記する
      - フィルタ適用後の一覧・打ち切りフラグ計算を、レンダー中に外側の`let`変数を書き換える実装から`useMemo`＋`reduce`で完結する実装に直した（oxlintの`react(immutability)`警告を受けて修正）
    - `npx tsc --noEmit`・`npx vitest run`（102件）・`npm run lint`・`npm run build`すべて通過。Playwrightで、プログラム未選択時は総合文化・実践教育科目の区分のみ表示され、メディア情報学プログラムを選択すると理数基礎・類共通基礎・類専門の区分が追加で現れること、学年=2年・学期=前学期で絞り込むと該当区分だけに絞られること、キーワード「複素関数論」で1件だけヒットすることを確認済み（consoleエラー無し）

53. **ベータテスト由来のメイン画面UI改善を実装（2026-09-07、開発者が実装を依頼）**：
    - `SubjectStatusSelect.tsx`を3択の`select`から「未履修・修得・不合格」のラジオボタン群へ変更した。少数の選択肢で現在の選択状態を一覧中でもすぐ比較できるようにした一方、選択肢の多い学期フィルタやプログラム選択は従来どおりドロップダウンのままとし、操作性と一覧性のバランスを取った
    - 「不可の単位」を「不合格になった科目」へ改め、必修の不合格には「必修：再履修して単位を修得してください」、選択科目の不合格には「選択：再履修するか、同じ区分から別の科目を選べます」という状態別の案内を表示するようにした。これにより、必修の再履修と選択科目の不足を区別できる
    - 必修一覧・選択科目一覧に補足説明を追加し、選択科目の各区分では未充足時に「あとN単位」と「この一覧からあとN単位を選んで修得してください」を表示するようにした。既に充足済みの区分にも充足済みであることを表示する
    - `MainPage.tsx`の科目行を共通の`SubjectRow`へ整理し、科目名・単位・学期・状態・曜日時限をグリッドで整列させた。`index.css`では操作部の最小高を広げ、スマホ幅では科目行が縦に並ぶようにした
    - `npm test`（104件）・`npm run lint`・`npm run build`すべて通過。ブラウザ上で必修の`Academic Written English I`を不合格にして更新し、不合格一覧に再履修の案内と不合格状態が表示されることを確認済み

54. **プロフィール設定の余白を調整（2026-09-07、開発者が「選択ボックスが大きすぎて窮屈なので、行同士を離したい」と依頼）**：
    - `SetupPage.tsx`の`main`と`form`に専用クラスを追加し、`index.css`で設定画面にだけ適用するレイアウトを定義した。ほかの画面のボタン・選択欄の大きさには影響しない
    - フォームの各入力グループをグリッド化し、項目間を`1.35rem`、ラベルと選択欄の間を`0.4rem`空けた。転類・転プログラムの追加入力も同じ間隔で並ぶ。選択欄の高さはタップしやすさのため維持し、横幅は画面を超えないようにした
    - ブラウザでⅠ類メディア情報学プログラムの設定画面を確認し、ラベル・選択欄・チェックボックスの各行が読み分けやすくなったことを確認。`npm run lint`・`npm run build`も通過（buildの既知のチャンクサイズ警告のみ）

55. **残タスク一覧と引き継ぎ参照を更新（2026-09-07、開発者が依頼）**：
    - `AGENTS.md`と`CODEX.md`の残タスクを、完了済みのデータ入力・設計判断・ベータテスト由来UI改善を除いた3項目（類専門科目85件のPDF目視確認、マシンデザインBの未確定クラス割り当て、他年度入学者への対応判断）へ整理した
    - 既に実装済みの内容に合わせ、科目マスタ数・クラス割り当ての記述・メイン画面の不合格表示とラジオボタンの記述を更新した。`docs/HANDOVER.md`と`docs/PENDING_YEAR_SEMESTER_CHECKS.md`に残っていた存在しない`CLAUDE.md`への参照も、現行の文書へ修正した

56. **不合格科目を要件区分ごとに表示（2026-09-07、ベータテストで「理数基礎（必修）の不足と、下方の不合格科目との対応が分かりにくい」と報告）**：
    - `MainPage.tsx`で、不合格科目にも取得単位・残りの必修と同じ区分分けを適用した。これにより「理数基礎（必修）」のような見出しの直下で、該当する不合格科目を確認できる
    - 各見出しには`現在X/Y単位・あとZ単位`を併記する。必修なら再履修が必要、選択科目なら別科目で補えるという既存の行内案内は維持する
    - `npm test`（104件）・`npm run lint`・`npm run build`すべて通過（buildの既知のチャンクサイズ警告のみ）

57. **トップページをβ2表記へ更新（2026-09-07、開発者が依頼）**：
    - タイトルを`電通大 単位取得ルートナビ(ver.β2)`へ変更し、非公式サイトである旨の注意書きの下に「リリースノート」「アップデート(β→β2)」「サイトのデザインを一新しました」を追加した
    - `npm run lint`・`npm run build`を通過（buildの既知のチャンクサイズ警告のみ）

58. **登録科目数とその他単位認定の科目数入力を追加（2026-09-08、開発者が依頼）**：
    - 取得単位の上に「登録科目数」を追加。修得・不合格の科目コード数を合計するので、同じコードの再履修は1科目として数える
    - その他単位認定には、既存の単位数プルダウンと並べて科目数プルダウン（0〜8科目）を追加した。科目に紐付かない認定分も、ここで指定した数だけ登録科目数へ含める
    - localStorage・更新・リセット・JSONダウンロード/読み込みにも科目数を対応させた。書き出し形式をschemaVersion 3へ上げ、version 1・2の既存ファイルも読み込めるように維持した。importersの単体テストを追加し、`npm test`（105件）・`npm run lint`・`npm run build`すべて通過（buildの既知のチャンクサイズ警告のみ）

59. **類専門科目85件の標準年次・学期をPDF画像で目視確認し、15件を修正（2026-09-08、開発者が確認作業を実施）**：
    - `docs/PENDING_YEAR_SEMESTER_CHECKS.md`の候補85件をCSVに切り出し、開発者が学修要覧2025のPDF画像を見ながら標準年次・学期を記入した。70件は既存値が正しく、15件だけ修正が必要だった
    - 修正対象は`FGN301e`・`FGN401e`、`PHY502g`・`PHY502h`、計測・制御システムプログラムの`MCE502i`・`MCE601i`・`MCE503i`・`MCE602i`、先端ロボティクスプログラムの`MCE503j`・`MCE601j`・`MCE504j`・`MCE602j`、機械システムプログラムの`MCE402k`・`MCE602k`・`MCE609k`。表示データと`gen_data.py`の生成元を同じ値にそろえたため、将来のデータ再生成でも修正が失われない
    - `py`/`python`が未設定の実行環境だったため、同梱Pythonで`validate_data.py`を実行して科目1391件・課程16件の整合性を確認した。`npm run build`も通過（既知のチャンクサイズ警告のみ）

60. **電子工学実験第一の曜日時限とシラバスリンクを学籍番号の偶奇で解決（2026-09-08、開発者が不具合を報告）**：
    - A5時間割では電子工学プログラムの電子工学実験第一が、水曜2〜4限（学籍番号奇数）と木曜2〜4限（学籍番号偶数）の2枠に分かれていた。従来は両方をプログラム全員向けとしていたため、6時限を同時に表示し、シラバスも一意に決められなかった
    - `class_assignment_filled.csv`と派生ファイルを偶奇別の対象へ更新した。1年次クラス番号の偶奇が学籍番号の偶奇に一致する既存ルールを用い、単独プログラムの「プログラム名の学籍番号奇数/偶数」表記にも対応した
    - `validate_data.py`・`classAssignment.test.ts`（32件）・`npm run build`を通過。奇数なら水曜2〜4限、偶数なら木曜2〜4限の1つのofferingに絞られるため、対応するシラバスURLもリンク表示される

61. **プロフィール設定から推薦入学の入力を削除（2026-09-08、開発者が依頼）**：
    - チェックボックスとプロフィールの保存項目を削除し、1年生は常にプログラム未定として扱うようにした
    - 既存のlocalStorageに残る`recommended`プロパティは、読み込み時に無視されるため設定の読み込みは壊れない
    - `docs/SPEC.md`の入力項目・プログラム配属時期の記述も新しい画面仕様に合わせた。`npm test`（106件）・`npm run build`を通過（既知のチャンクサイズ警告のみ）

62. **学年にかかわらず進学予定のプログラムを選べるように変更（2026-09-08、開発者が依頼）**：
    - 1年生のプログラム選択を無効化して未定へ固定する処理と案内文を削除した。1年生でも進学予定のプログラムを選んで、その要件・必修科目を確認できる
    - 「未定」は引き続き選択肢として残し、プログラムを決めていない学生は類共通要件だけを確認できる。`npm test`（106件）・`npm run build`を通過（既知のチャンクサイズ警告のみ）

63. **学修要覧2025・2026を比較し、2026年度データを年度別に生成（2026-09-08、開発者が時間をかけた調査と新年度データ作成を許可）**：
    - 両PDF全54ページを対応させ、卒業要件・審査条件と付録Cの科目表を、文字抽出とページ画像の目視を併用して比較した。別表2の卒業所要単位とサイトで使う審査数値は、昼間15プログラムすべてで変更なし。科目表では、海外研修Ⅰ・Ⅱの追加、情報数理工学のMTHa/MTHb系列再編、マルチメディア処理の重複番号解消、Ⅲ類のサイエンス工房追加とUECパスポート廃止、機械のMCEb系列繰り上げ、化学生命工学特論のA/B分割などを確認した。詳細は`docs/YOURAN_2026_COMPARISON.md`へ記録した
    - `scripts/build_2026_data.py`を追加し、2025年度データを土台に確認済み差分だけを適用して、`data/requirements/2026-*.json`17件と`data/subjects/youran-2026.json`を再現可能に生成するようにした。変更のない科目は既存の2026年度シラバス情報を引き継ぎ、新設・番号変更科目は誤リンクを避けるためofferings未設定とした
    - 比較中に、2025・2026の情報通信工学プログラム表にある`ELE604g`・`ELE605g`・`ELE606g`・`PHO601g`・`ELE607g`が、既存の2025科目マスタとⅡ類要件から漏れている不具合を発見した。5科目を科目マスタ、Ⅱ類5プログラムの選択科目、`scripts/gen_data.py`へ追加し、再生成でも消えないようにした
    - `scripts/validate_data.py`を2025・2026の両年度同時検証へ拡張し、科目参照、必修単位、小計、審査条件に加えて、確定した2026固有の追加・廃止科目も回帰検査するようにした。検証結果は2025年度1396科目・16課程、2026年度1394科目・16課程で正常
    - 2026年度データはまだ画面へ配線していない。現在の画面は科目マスタを1年度分だけ読み込むため、年度混在を避けるには科目一覧・詳細を含む年度別切り替え、新設科目のシラバス取得、代表差分の画面テストをまとめて行う必要がある。残タスクをこの内容へ更新した

64. **2026年度データを画面へ配線（2026-09-08、開発者が作業継続を依頼）**：
    - `requirementSets.ts`で要件・科目マスタを入学年度ごとのMapとして保持し、要件計算・単位数取得・科目検索・要件上の位置づけ検索が選択年度だけを見るよう変更した。これにより、同じフルコードでも年度が違えば別科目として扱える
    - プロフィール設定で2025年度と2026年度を選択可能にし、年度変更時はプログラム選択をいったん未定へ戻して、異なる年度の設定を誤って引き継がないようにした。メイン画面の科目リンク、科目一覧、科目詳細も年度をURLクエリで保持する
    - 単体テストで`MTHb01c`の年度別名称、2026年度の海外研修、2026年度固有科目の要件上の位置づけを検証した。ブラウザでも同じ`MTHb01c`が2025年度では「ハイパフォーマンスコンピューティング基礎論」、2026年度では「シミュレーション理工学基礎論」と表示されることを確認した
    - `python scripts/validate_data.py`、`npm test`（109件）、`npm run lint`、`npm run build`を通過。2026年度の新設・番号変更科目で未確認の開講情報は、誤リンクを避けるため非表示のままとした
