# AGENTS.md — このリポジトリで作業するときのルール

## プロジェクト概要

電気通信大学（UEC）情報理工学域の学生向けに、「学年・類・プログラム・取得済み科目」を入力すると
卒業要件の充足状況と、残りの必修・区分別の不足が一目で分かる静的Webサイトを作る。
**仕様はすべて `docs/SPEC.md` に書いてある。迷ったらまず SPEC.md を読むこと。**

## 開発者について

- 開発者は大学2年生で、開発経験はほとんどない。C++ は書けるが TypeScript/React は初めて
- したがって、生成するコードには**日本語のコメント**で「このファイルは何をするか」「この関数は何をするか」を書く
  - **粒度は「1スコープ（関数・if/elseブロック・ループなど）につき最低1つ」**。ファイル冒頭や
    トリッキーな箇所にだけ書くという一般的な作法（非自明な処理にだけコメント）はこのプロジェクトでは
    不十分と指摘された（2026-09-04）。`*.test.ts`のdescribe/itブロックも対象（describeには
    「何を検証する分類か」、itには「なぜこの結果になるか」を一言）
- TypeScript や React で初めて出てくる構文・概念は、コード中のコメントか作業後の説明で一言補足する
- 一度に大量のファイルを生成しない。`docs/SPEC.md` §11 のフェーズ単位で進め、各フェーズの終わりに何を作ったか要約する
- **チャット上（コンソール）で「Ⅰ類」「Ⅱ類」「Ⅲ類」と書くときは、ローマ数字と「類」の間に半角スペースを入れる（「Ⅰ 類」「Ⅱ 類」「Ⅲ 類」）**。詰まって見えるとの開発者の指摘（2026-09-06、複数回指摘済み）。**サイトのコード・表示・コミットメッセージ等はこの限りでなく、従来通りスペース無しでよい**。あくまでCodexが会話で表記するときだけのルール

## 技術構成（docs/SPEC.md §9）

- TypeScript + React + Vite。ルーティングは React Router の HashRouter（GitHub Pages 対応）
- 状態は React state + localStorage。サーバー・DB・認証は使わない
- テストは Vitest。`src/domain/` のロジックには必ず単体テストを付ける
- データ更新スクリプトは Python（`scripts/`）
- ホスティングは GitHub Pages（GitHub Actions で自動デプロイ）

## コード構成のルール

- `src/domain/` は**純粋なロジックのみ**。React にも DOM にも依存させない（後で C++ 版と突き合わせられるように）
  - `requirements.ts` … 要件充足の判定（SPEC §5 F-3, §7.2「充足計算の順序」）
  - `recommend.ts` … 残り・推奨の算出（SPEC §8）
  - `importers.ts` … JSON 取り込み（SPEC §7.4, §7.5）
- `data/` の JSON が唯一のデータソース。コード中に科目名や単位数をハードコードしない
- `data/` を変更したら `python scripts/validate_data.py` が通ることを確認する
- 科目の主キーは**末尾記号を含むフルコード**（例 `COM405a`）。以前は末尾記号を除いて名寄せしていたが、類専門科目の選択科目のようにプログラムごとに独自採番されている科目では、末尾を除くと番号が一致しても別科目になるケースがあると判明したため撤回した（2026-09-04）。`COM405a`/`COM405e`のように複数プログラムで本当に共有されている科目は、単純に別エントリとして重複して持つ

## データについて（docs/SPEC.md §3, §7）

- `data/requirements/2025-day-common.json` … 総合文化・実践教育科目の要件（昼間コース共通）
- `data/requirements/2025-day-I-media.json` … Ⅰ類メディア情報学プログラムの専門科目要件と審査条件。`extends` で common を参照
- `data/subjects/youran-2025.json` … 学修要覧2025 付録C から起こした科目マスタ（曜日時限・シラバスURLは未収録。シラバスから取る）
- これらは学修要覧2025の原本と突き合わせて検証済み。**数値を勝手に変えない**。疑問があれば開発者に確認する
- 他のプログラム・他年度の要件ファイルは、同じ形式で後から追加する
- **`scripts/fetch_syllabus.py`は科目名が科目マスタ（`gen_data.py`のハードコードした表）と完全一致する
  行しか個別ページを取得しない**（全件を舐めるとサーバー負荷が大きいため）。つまり、シラバスに
  実在する科目でも科目マスタに載っていなければ絶対に自動取得されない。「シラバスにはあるのにサイトに
  出ない」科目を調査するときは、まず`data/subjects/youran-2025.json`（または`gen_data.py`の該当リスト）に
  その科目が入っているかを確認する（2026-09-07、論理学(HSS321z)の欠落で発覚。学修要覧2025の付録Cに
  掲載が無い＝2025年度版印刷後に新設された科目である可能性があるので、原本ページも確認した上で追加する）
- 学修要覧PDF（`PDF/tanni_extract_final.pdf`、リポジトリには含めない）を読む具体的な手順（poppler/
  pdftoppmのパス、Playwrightでの代替手段、別表2の数値をpdftotext -layoutの列位置較正で機械的に
  抽出する手法など）は`docs/PDF_READING_NOTES.md`を参照

## やらないこと（docs/SPEC.md §1.4）

- 学務情報システムへの自動ログインやスクレイピング
- CAP制（履修上限）のチェック
- 時間割グリッド表示（友人の時間割アプリが担当）
- シラバス本文の転載（メタデータとリンクのみ）

## 作業の進め方

- フェーズが終わるごとに `git commit`。コミットメッセージは日本語で「何をしたか」を1行
- 仕様と違うことをする必要が出たら、勝手に変えずに理由を説明して開発者に確認する
- 外部サイト（`kyoumu.office.uec.ac.jp`）へアクセスするスクリプトは、1秒以上の間隔を空け、User-Agent に連絡先を入れる
- `scripts/fetch_syllabus.py`は昼間・夜間主あわせて数百件の個別ページを1.2秒間隔で取得するため、
  フル実行には10〜15分程度かかる。進捗表示は標準エラー出力なので、実行環境によっては完了するまで
  何も出力されないように見えることがあるが、それ自体は異常ではない（ハングしたと判断して中断しない）
- 開発者に作業用ファイル（レビュー用CSV等）を渡すときは、チャットの添付機能ではなく
  `C:\Users\maita\Downloads\` 直下に直接コピーして「Downloadsフォルダを見てください」と伝える
  （開発者はローカルPC上で作業しており、添付ファイルの開き方に不慣れなため）。本体ファイルを
  削除・移動するのではなく、常にその時点のコピーを渡し、本体（`data/timetable/class_assignment_filled.csv`等）は
  そのまま保持する
- **ブランチ運用（2026-09-06〜、サイト公開に伴い変更）**：`main`へのpushは`.github/workflows/deploy.yml`でGitHub Pagesへの自動デプロイをトリガーする。通常の開発作業は`dev`ブランチで行い、`dev`へのコミット・プッシュは（開発者の指示により）確認なしで進めてよい。`main`へのマージ（＝実際に公開サイトが更新される操作）は、コミット・プッシュとは別の重みを持つ操作なので、マージする前に開発者に確認する

## 現在の状態（要約、2026-09-07時点）

**詳しい経緯（いつ・なぜ・どう直したか）は `docs/PROGRESS_LOG.md` に時系列で全部残してある。** ここには「今どうなっているか」だけを書く。同じ種類の不具合を調べるとき・過去の判断の理由を知りたいときはPROGRESS_LOG.mdを検索すること。

### データ

- 昼間コース Ⅰ類5・Ⅱ類5・Ⅲ類5プログラム＋夜間主課程、計16課程分の要件データ（`data/requirements/`）が完成。全課程で必修単位・審査条件の単位数を学修要覧原本（`PDF/tanni_extract_final.pdf`、リポジトリには含めない）と照合済み
- 審査（2年次終了時審査・卒業研究着手審査・卒業審査等）データも全16課程で完成（`reviews`）
- 科目マスタ（`data/subjects/youran-2025.json`、1388科目）に、2026年度シラバス（`kyoumu.office.uec.ac.jp/syllabus/2026/`）から取得した`offerings`（曜日時限・担当教員・シラバスURL）を可能な限り付与済み。大学院連携科目・卒業研究など性質上シラバスに出ない科目、および原因未特定の少数は未取得のまま
- 複数セクション（クラスごとに教員・時限が違う）科目は、`data/timetable/class_assignment_filled.csv`（開発者が時間割PDFを見ながら手作業で記入）→`class_assignment.json`で、プロフィールのクラス情報と突き合わせて一意に解決する仕組みが稼働中。記入待ちの残数・対象科目は下記「残タスク一覧」参照
- データ生成パイプラインの実行順序：`python scripts/gen_data.py`（科目マスタ・要件JSONを再構築。**offerings・prerequisitesTextを消してしまう**）→`python scripts/fetch_syllabus.py`（シラバスから曜日時限等を再取得）→`python scripts/build_class_assignment.py`（クラス割り当てCSVのスキャフォールドを再生成。既存の記入済みclass_idは引き継ぐ）→`python scripts/build_class_assignment_json.py`（CSV→JSON変換）→`python scripts/validate_data.py`（整合性チェック）

### 実装済みの画面・機能

- `/`（トップ）・`/setup`（プロフィール設定：入学年度・コース・類・プログラム・学年・クラス情報・転類/転プログラム情報）・`/main`（メイン画面）・`/courses`・`/courses/:id`（科目一覧・詳細）・`/route/:year/:cls/:prog`（固定ページ）・`/about`
- メイン画面：取得単位・不可の単位・残りの必修・選択科目・共通単位・その他単位認定・審査結果・その他の科目（転類/転プログラム前に必修だった科目用）を表示。科目ごとに曜日時限（クラス別に解決できた場合のみ）とシラバスへのリンク（一意に決まる場合のみ）を表示。ダウンロード・読み込み・リセットのツールバー
  - 2026-09-08、開発者判断で変更：不合格（不可）の科目は「不可の単位」に既に出るので「残りの必修」「選択科目」には重複して出さない（表示フィルタ（学期）を絞っても対象外）。複数offeringがある科目（物理学概論第一等）が不合格になった場合は、「不可の単位」の各行の真下にインデントして「※ 再履用の授業があります：{曜日時限}」という注記を出す（`resolveSlotsForProfile`にisRetaking=trueで解決）。この機能追加の過程で、`class_assignment_filled.csv`の`class_id`表記に「再履生のみ」という表記ゆれ（`classIdMatchesProfile`が認識する正式表記は「再履生」「再履全員」のみ）があり、PHY102z・PHY202z（物理学概論第一・第二）で再履修枠の曜日時限が出ない不具合になっていたのを修正した
  - 画面右上の「表示」（学期フィルタ）は、従来「次に取るべき科目」の推奨順にしか効いておらず、「残りの必修」「選択科目」の一覧は学期を変えても全学年分が表示されたままだった。`isVisibleForTermFilter()`を追加し両方の一覧に適用（2026-09-08）
- ドメインロジック（`src/domain/`、すべてReact/DOM非依存の純粋関数＋単体テスト）：`requirements.ts`（充足判定）・`recommend.ts`（推奨スコア、先修科目考慮）・`reviews.ts`（審査条件評価）・`classAssignment.ts`（クラス別セクション解決、シラバスリンクの絞り込みにも使用）・`prerequisites.ts`（先修科目の保守的抽出）・`importers.ts`（JSON入出力、schemaVersion 2）

### 2026-09-07に削除した機能

- `/compare`（プログラム比較、未実装のプレースホルダーのまま）・`/data`（全消去のみ、メイン画面のリセットボタンと完全に重複）：開発者の明示許可を得て削除済み

### 既知の制約・保留事項

- 留学生・一部の再履修関連の`class_id`表記（`留学生`）は、対応するプロフィール項目が無いため未対応（曜日時限が出ないだけで誤表示はしない）
- 入学年度は2025年度のみ対応。`SetupPage.tsx`の`SHOW_ENTRY_YEAR_INPUT`を`true`に戻せば入力欄が復活する
- 英語科目は担当教員が多数・頻繁に変わるため、シラバスリンク・曜日時限とも一意に決まらないことが多い（意図した挙動）
- その他の細かい残タスクは下記「残タスク一覧」を参照

## 残タスク一覧（2026-09-07時点、開発者の依頼で集約。同日中に随時更新）

これまでの進捗ログに散らばっていた申し送りを、優先度カテゴリ別にまとめ直したもの。個々の背景・経緯は括弧内の番号で`docs/PROGRESS_LOG.md`の該当項目を参照。

**① 開発者の入力待ち（データ）→ 2026-09-08にすべて解決**
- 27番「シラバス・データ全体の見直し」→ 2026-09-07に実施（41番）。`fetch_syllabus.py`をフル再実行し45科目が新たにoffering取得、`class_assignment`も監査済み。当初112行が`class_id`未記入だったが、44・45・47番のマージと46番のバグ修正（`build_class_assignment.py`のまとめ書き時限キー展開）を経て、34行（`GSE301g/h/i/j`・`MTH402i/j`・`ELE403h`）が残っていたが、2026-09-08に`class_id_worksheet_20260908.csv`の記入分をマージして解決（52番）
  - マージ時、複数プログラム共通科目の学籍番号偶数/奇数の書き方が開発者の記入（鉤括弧＋中黒区切り）のままだと`scripts/build_class_assignment_json.py`の`expand_parity_programs()`が期待する書式（読点区切り、鉤括弧なし）と合わず、＆連結変換がされないまま=誰にも曜日時限が表示されないバグになることをテストで発見・修正して記入
  - `MTH402i`(計測・制御システムプログラム)/`MTH402j`(先端ロボティクスプログラム)の`class_id`が「情報通信工学プログラム＆電子情報学プログラムの学籍番号偶数/奇数」になっている点は一見矛盾するが、確認の結果、同じ講義（同じtimetableCode）が`MTH402g`(情報通信工学)/`MTH402h`(電子情報学)としては類専門「選択必修」科目である一方、`MTH402i/j`としては類共通基礎の「自由」科目（卒業単位に数えない）扱いのため、学籍番号分担の対象は情報通信工学・電子情報学の学生で正しい。計測制御・ロボの学生には曜日時限が表示されないが、そちらでは自由科目なので実害なし
  - `MCE602i/j/k`（マシンデザインB）の「機械システムプログラムの一部も対象だが振り分け未定」は未解決のまま残っている（44番参照）
- （2026-09-07訂正：`kiso_kagaku_jikken_extract.csv`＝基礎科学実験A1/A2/B1/B2の記入待ちは、28番で「dedup」版が既にマージ済みで**解決済み**だった。`class_assignment_filled.csv`を確認したところ`PHY101z`/`PHY201z`/`CHM101z`/`CHM202z`は全行`class_id`が埋まっている。この一覧を作った時点で古い情報を参照してしまっていたミス。開発者の指摘で気づいた）

**② 設計判断待ち → 2026-09-07にすべて解決**
- 留学生・転類生（転プログラム）対応 → 2026-09-07に方針決定・実装完了（42番）。留学生は特に対応しない方針。転類・転プログラムはプロフィールにチェックボックス＋元の類/プログラム/クラスの入力を追加
- `/compare`（プログラム比較）・`/data`ページの削除 → 2026-09-07に開発者が明示許可し削除完了（49番）

**③ 未実装の機能 → 2026-09-07に全項目実装完了。詳細は40番参照**

**④ 保留中（先送りと決めたもの）**
- 入学年度入力欄（`SetupPage.tsx`の`SHOW_ENTRY_YEAR_INPUT`）：2024・2026年度データを追加する気になったら`true`に戻す。着手前に「カリキュラムが本当に年度で違うのか」を確認し、差が無ければデータ使い回しも検討する（33番）

**参考：実害の無い既知の残存事項**
- `offerings`だけが正当な理由でPDFの標準学期とズレている5件（`PHY204f/g/h`・`PHY502g/h`・`MTH502f`）。曜日時限が非表示になるだけで誤表示はしないため緊急ではない（39番。**`ELE301i`はここに含まれていたが51番でこの判断自体が誤りだったと判明し、実際に修正したため除外した**）
- 類専門科目の標準年次・学期についてPDF照合がまだ済んでいない85件 → `docs/PENDING_YEAR_SEMESTER_CHECKS.md`参照（51番）
