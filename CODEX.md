# Codex 作業引き継ぎメモ

Codexがこのリポジトリで作業を始めるときの短い入口です。詳細な仕様は`docs/SPEC.md`、作業規則と現時点の制約は`AGENTS.md`、過去の判断の理由は`docs/PROGRESS_LOG.md`を参照してください。このファイルはそれらを置き換えません。

## 現在の状態（2026-09-08時点のデータ）

- 2025年度入学生向けの要件は、昼間コースのⅠ類・Ⅱ類・Ⅲ類の各5プログラムと夜間主課程、計16課程に対応している。科目マスタは1,396科目ある。
- 学修要覧2025・2026を比較し、2026年度用の要件17ファイルと科目マスタ1,394科目を生成済み。プロフィール・メイン画面・科目一覧・科目詳細は入学年度に応じてデータを切り替える。確定差分は`docs/YOURAN_2026_COMPARISON.md`、再生成は`scripts/build_2026_data.py`を参照。
- クラス別の開講情報は`data/timetable/class_assignment_filled.csv`から`class_assignment.json`へ変換済みで、CSVの`class_id`未記入行は0件である。
- 標準年次・学期は全課程を点検し、候補85件も開発者がPDF画像で目視確認して修正済み。
- `src/domain/`には卒業要件・推奨・審査・先修科目・クラス別開講情報・JSON取り込みの純粋ロジックと単体テストがある。UIはプロフィール、メイン、科目一覧・詳細、固定ルート、Aboutまで実装済みである。

## 最初に確認するもの

1. `AGENTS.md`：作業規則。特に日本語コメント、データの扱い、ブランチ運用を守る。
2. `docs/SPEC.md`：機能やデータモデルの正しい仕様を確認する。
3. `docs/PROGRESS_LOG.md`：既存の判断や同種バグの修正経緯を`rg`で検索する。
4. `docs/PENDING_YEAR_SEMESTER_CHECKS.md`：標準年次・学期を変更する前に、保留候補か確認する。

## 整理済みタスク（2026-09-08時点）

### 現在の確定タスク

1. **ベータテストで新しく見つかった不具合を都度修正する**
   - 単位計算、要件上の区分、曜日時限、リンク先、画面の読みやすさを優先して確認する

### 将来の改善候補（未着手・実装は任意）

1. **審査の詳細を行動ベースにする**
   - 不足条件の列挙に加えて、「まず○○区分をあとN単位」「この必修科目を修得」のように、次の行動を優先順で示す
2. **ビルド後のJavaScriptを分割する**
   - 現在はビルドに成功しているが、ファイルサイズ警告が出ている。画面単位の読み込み分割を検討する

### 完了済みとして一覧から外した項目

- 「修得予定」状態、見込み計算、予定科目一覧、時限重複の注意
- クラス情報が未設定・候補を絞れない場合の曜日時限の案内
- プロフィールでクラス情報が必要な理由の説明
- リリースノートでの機能変更・見た目の変更の分類

## データを変更するとき

科目名や単位数をコードへ直接書かず、`data/`を唯一のデータ源にする。学修要覧の数値は推測で変更しない。

曜日時限・担当教員・シラバスURLなどの開講情報は、原則として**2026年度の公式シラバス**を参照する。`youran-2025.json`の「2025」は入学年度のカリキュラム版であり、開講年度を表すものではない。次年度へ更新するときだけ、参照する公式シラバス年度と時間割データをまとめて見直す。

データ生成パイプラインは次の順序で実行する。`gen_data.py`は`offerings`と`prerequisitesText`を消すため、単独で実行して終わりにしない。

1. `python scripts/gen_data.py`
2. `python scripts/fetch_syllabus.py`（大学サイトへアクセスするため、完走に10〜15分かかる）
3. `python scripts/build_class_assignment.py`
4. `python scripts/build_class_assignment_json.py`
5. `python scripts/validate_data.py`

学修要覧PDFは現在`PDF/yoran_2025.pdf`にあり、Git管理しない。読み方は`docs/PDF_READING_NOTES.md`を参照する。

## 作業後の確認と記録

- TypeScript/Reactの変更後：`npm test`、`npm run lint`、`npm run build`
- `data/`の変更後：上記に加えて`python scripts/validate_data.py`
- 一つのフェーズを終えたら、日本語の1行コミットを`dev`ブランチへ作る。`main`へのマージは公開操作なので、開発者の確認なしに行わない。
- 仕様・設計・未解決事項が変わったときは、この「現在の状態」と該当する詳細文書も一緒に更新する。作業の経緯は`docs/PROGRESS_LOG.md`へ追記する。

## 直近の記録

- 2026-09-07：残タスクを、完了済みのデータ入力・設計判断・UI改善を除いた3項目へ整理した。
- 2026-09-07：プロフィール設定の入力行の間隔を広げた。選択欄の高さは保ち、設定画面だけでラベル・選択欄・各項目の区切りを明確にしている。
- 2026-09-08：このファイルを追加。実在するPDF名、クラス割り当ての変換済み状態、実装済み画面の状況に合わせて、README・構成説明・時間割データ説明を更新した。
- 2026-09-08：入学年度別の要件・科目マスタ切り替えを画面へ配線し、同一の科目番号でも年度ごとに正しい名称を表示するようにした。
- 2026-09-08：β2.4.1までをmainへマージした。修得予定、共通単位の内訳、他類専門科目認定、開講情報表示、日本文化Ａ〜Ｅの並び順を反映済み。

## ベータテスト由来のUI改善（実装済み、2026-09-07）

ベータテストで出た「必要な行動が分かりにくい」「操作部が小さく、一覧の整列も弱い」という意見に対応した。卒業要件の判定ロジックや保存形式は変更していない。

1. 各科目の履修状態を、3択のドロップダウンから「未履修・修得・不合格」のラジオボタンに変更した。選択肢の多い学期フィルタやプログラム選択などはドロップダウンのまま維持している。
2. 「不可の単位」を「不合格になった科目」に改め、必修の不合格には「必修：再履修して単位を修得してください」と明示する。選択科目の不合格には、再履修または同一区分の別科目を選べることを明示する。
3. 必修一覧と選択科目一覧に説明文を追加した。必修の不合格は上の不合格一覧で確認すること、選択科目の区分では残り単位数を選んで修得することが分かる。
4. 科目行・操作部・ツールバーをグリッドとまとまりのある配置に整理し、ボタンとラジオボタンを大きくした。狭い画面では科目行が縦方向に並ぶレスポンシブ表示にしている。
5. `npm test`（104件）・`npm run lint`・`npm run build`を通過し、ブラウザで必修科目を不合格に変更したとき、再履修の案内と状態変更が表示されることを確認した。
6. 2026-09-07：不合格一覧も要件区分ごとに分けた。各見出しに現在単位・必要単位・不足単位を表示し、例えば理数基礎（必修）の不足と、その区分で不合格になった科目を対応付けて確認できる。
7. 2026-09-08：取得単位の上に「登録科目数」を表示する。修得・不合格の科目コードを合計し、同じ科目の再履修は重複させない。その他単位認定には単位数とは別の科目数入力を設ける。
