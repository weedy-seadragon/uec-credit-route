// 画面（UI）はフェーズ2以降で実装する。
// 現時点ではVite+React+TypeScriptの初期化確認用の最小限の表示のみ。
function App() {
  return (
    <main>
      <h1>電通大 単位取得ルートナビ</h1>
      <p>準備中（フェーズ2で画面を実装予定）</p>
    </main>
  )
}

export default App
