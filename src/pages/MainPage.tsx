// メイン画面（"/main"）。取得／不可／残りの必修／区分別進捗を表示する（F-3, F-4）。
// フェーズ2-4で実装する。このアプリの中心になる画面。
import PagePlaceholder from '../components/PagePlaceholder'

export default function MainPage() {
  return <PagePlaceholder title="メイン画面" note="準備中（フェーズ2-4で実装予定）" />
}
