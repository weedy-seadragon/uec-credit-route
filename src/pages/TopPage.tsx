// トップページ（"/"）。説明・免責・「はじめる」ボタンを置く予定（docs/SPEC.md §6）。
import PagePlaceholder from '../components/PagePlaceholder'

export default function TopPage() {
  return <PagePlaceholder title="電通大 単位取得ルートナビ" note="準備中（フェーズ2で実装予定）" />
}
